# Fraud Golden Set v0.1

## 출처와 권리

- `fraud_golden_v0.1.jsonl`은 FinShield 팀이 2026-08-13 직접 작성한 합성 문장이다.
- 실제 피해자 메시지, 개인정보, 외부 데이터셋 문장을 복사하지 않았다.
- 외부 데이터 라이선스나 재배포 권한에 의존하지 않는다.
- `synthetic: true`가 아닌 사례는 v0.1 로더가 거부한다.

## 라벨 계약

각 줄은 독립 JSON 객체이며 다음을 포함한다.

- `case_id`: 변경되지 않는 `fg-NNN` 식별자
- `text`, `persona`, `state`, 선택적 `url`: API 입력
- `is_fraud`, `expected_fraud_types`: 이진·유형 라벨
- `required_signal_codes`: 반드시 노출돼야 하는 public signal
- `expected_min_risk`: 최소 위험 등급
- `required_action_codes`: 사용자 상태상 반드시 필요한 행동
- `annotation_note`: 판정 이유와 경계 조건

라벨의 사기 유형·행동 코드는 실제 API 계약에 존재해야 하고 중복될 수 없다.
모든 `UserState`가 포함돼야 하며 사례 ID는 유일해야 한다.
평가 결과에는 순서와 라벨을 포함한 정규화 사례의 SHA-256을 기록해 어떤 데이터로
측정했는지 식별한다.

## 이용 한계

이 데이터는 합성 61건의 bootstrap 개발셋이며 독립 held-out 테스트셋이 아니다.
각 `UserState`는 최소 3건을 포함하지만 `received_only`가 43건으로 많아 균형
표본은 아니다. 엔진 오류를
확인하고 규칙을 수정하는 데 사용했으므로 이 결과를 실서비스 탐지 성능이나
일반화 성능으로 주장하면 안 된다. v0.2는 이 파일을 동결한 뒤 별도 작성자 검수와
미사용 held-out 표본을 추가한다.

---

# Injection Golden Set v0.1

`injection_golden_v0.1.jsonl`. 로더는 `evaluation/injection_golden.py`.

## 무엇이 다른가

**사기 골든셋은 판정을 잰다. 이쪽은 경계를 잰다.** 묻는 것이 "이 문자가 사기인가"
가 아니라 "이 문자에 심긴 지시가 프로바이더까지 도달하는가" 이고, 그 질문은 모델을
부르지 않고 답할 수 있다. 그래서 이 셋의 검사는 CI 에서 돈다
(`tests/test_llm_prompt_injection.py`). **유료 호출이 없다.**

모델이 실제로 지시를 따르는지는 별개의 유료 측정이며, 날짜와 모델을 붙여
`docs/devlog/2026-08-20/prompt-injection-boundary.md` 에 남긴다. 그 숫자를 여기
두면 CI 가 매번 돈을 쓰거나, 낡은 숫자가 테스트인 척하게 된다.

## 라벨 계약

- `case_id`: `pi-NNN`
- `technique`: `direct_override` · `role_assignment` · `english_mixed` ·
  `delimiter_forgery` · `verdict_forgery` · `developer_impersonation` ·
  `output_pinning`
- `text`: 주입 문장이 섞인 문자 원문
- `injected_fragment`: **프로바이더까지 가면 안 되는** 조각
- `evidence_fragment`: **반드시 남아야 하는** 조각. 이쪽이 사라지면 설명이 근거를
  잃는다
- `expected_min_risk`, `note`

두 fragment 는 로더가 `text` 안에 실제로 있는지 검사한다.

## 출처와 권리

FinShield 팀이 2026-08-20 직접 작성한 합성 문장이다. 실제 피해자 메시지나 외부
데이터셋을 복사하지 않았다.

## 이용 한계

7건짜리 개발셋이다. held-out 아니고, 기법 목록이 완전하지도 않다. 실제로 첫
실행에서 3건(`pi-005`·`pi-006`·`pi-007`)이 뚫렸고 그만큼 패턴을 넓혔다 — 즉 이 셋은
**현재 방어를 교정하는 데 쓴 셋**이므로 통과율을 방어 성능으로 주장하면 안 된다.

`pi-004`(구획 위조)는 입력 계층이 **의도적으로 막지 않는다.** 명령문이 아니라
평서문이라 잡으려면 "금융감독원" 이 든 문장을 지워야 하고, 그러면 금융감독원을
사칭한 실제 사기 문자를 통째로 지운다. 이 건은 `validation.py` 의 근거 이탈 검증이
받는다.

> **2026-08-20 갱신.** `pi-002`의 결정론 판정이 `low` 였던 것은 주입과 무관한
> **탐지 공백**이었고 `docs/32` 소관으로 넘겼다. v0.3 엔진 수정에서 은행 자칭
> 어휘를 요구 조건과 함께 넣으면서 `medium` 으로 올라갔고, 골든 레이블도 그에
> 맞춰 고쳤다. 이 셋의 `expected_min_risk` 는 **주입 방어가 아니라 탐지 성능**을
> 기록한다는 점을 기억해야 한다 - 엔진이 좋아지면 이 값도 따라 움직인다.

---

# Fraud Holdout Set v0.2

`fraud_holdout_v0.2.jsonl`, 72건. 로더는 `evaluation/fraud_golden.py` 를 공유한다
(`load_holdout_cases`).

## 왜 파일을 나눴나

v0.1 개발셋은 엔진 오류가 0건이 되어 **변별력을 잃었다.** 오답이 나오면 어휘를
고쳐 왔으니 당연한 결과이고, 그래서 그 셋의 점수는 더 이상 성능이 아니라 기억이다.

같은 파일에 `held_out` 플래그만 다는 방법도 있었지만 쓰지 않았다. 한 번의
실수로 held-out 사례가 규칙 수정에 쓰이고, 그 사실이 아무 데도 안 남는다.
**경계는 코드가 아니라 파일이 지킨다.** 로더는 한 파일이 반쪽만 held-out 인 것과
`fh-` 사례가 `fg-` ID 를 달고 들어오는 것을 둘 다 거부한다.

## 라벨 규칙

v0.1 계약을 그대로 쓰고 `held_out: true` 하나가 는다. 판정이 갈리는 자리는 아래
규칙으로 고정했고, **측정 전에** 정했다.

- `is_fraud` 는 **문자 자체**가 사기 시도인지만 본다. 사용자 상태와 무관하다.
- `expected_min_risk` 가 `high` 인 경우는 둘 뿐이다. 돈·인증정보·접근수단·설치·
  원격제어를 **명시적으로 요구**했거나, 상태가 `shared_account_access` 이상인 것.
  사칭·압박·링크 유도만 있고 구체적 요구가 없으면 `medium` 이다.
- `required_signal_codes` 에는 **문장에 실제로 드러난 것만** 적는다. 도메인
  평판처럼 바깥 지식이 있어야 아는 것은 요구하지 않는다. `fh-004`·`fh-009` 가
  그래서 링크 신호를 요구하지 않는다 — 요구하면 하지 않기로 한 기능
  (`10-mvp-backlog.md` P1 outbound-fetch)을 못 했다고 감점하는 셈이 된다.
- `required_action_codes` 는 상태 정책이 이미 주는 것과 신호에서 나와야 하는 것을
  함께 적는다.

> **2026-08-20 정정.** 위 마지막 줄은 원래 "교집합으로 채점하므로 중복은 무해하다"
> 였는데 절반만 맞다. `_required_action_coverage` 는 교집합이라 무해하지만,
> `_state_policy_accuracy` 는 `set(required) <= actions` 즉 **부분집합**을 요구한다.
> 하나라도 더 적으면 그 사례는 상태 정책 오답이 된다. v0.2 의 상태 정책 정확도
> 0.681 에는 이 라벨 착오분이 섞여 있을 수 있고, 얼린 파일이라 고치지 않는다.
> v0.3 은 이 사실을 알고 라벨했다.

## 무엇을 일부러 넣었나

- **부정 28건.** 오탐률을 재려면 정상 문자가 있어야 한다. 이 중 몇 건은 사기
  문자와 표면 어휘가 겹치도록 일부러 골랐다 — `fh-064`(서류를 "전달해"),
  `fh-063`("다시 보내"), `fh-068`(본인이 은행 앱을 "설치").
- **상태만 위험한 정상 문자 5건**(`fh-068`~`fh-072`). 상태가 탐지로 새는지 본다.
  주의 등급은 올라가야 하지만 사기 유형이 붙으면 안 된다.
- **예방 안내문 5건**(`fh-057`~`fh-061`). 억제 규칙이 정확히 겨냥하는 형태다.
- **`fh-040`** 은 필수 신호가 비어 있다. 본문만으로는 정상 청첩장과 구분되지
  않는 경계 사례라 신호를 요구하지 않고 남겨 뒀다.

## 출처와 권리

FinShield 팀이 2026-08-20 직접 작성한 합성 문장이다. 실제 피해자 메시지, 개인정보,
외부 데이터셋 문장을 복사하지 않았다. 문장은 공개된 사기 **유형**을 참고해
새로 썼다.

## 이용 한계

**저자가 엔진 어휘를 이미 알고 있다.** 완전한 blind 셋이 아니다. 그래서 지킨 것은
순서다 — 72건을 모두 쓰고 커밋해 동결한 **뒤에** 처음 돌렸다. 커밋 이력이 그
순서를 증거로 남긴다.

합성이고, 72건이며, 한국어 문자·메신저 형식에 한정된다. 음성·통화 녹취는 없다.
분포도 실제 수신 비율이 아니다(`received_only` 45건). 이 숫자를 실서비스 탐지
성능으로 주장하면 안 된다. **가치는 절대값이 아니라 개발셋과의 차이에 있다.**

CI 는 이 셋에 **성능 기준선을 걸지 않는다.** 걸면 빨개질 때마다 이 셋을 보고
규칙을 고치게 되고, 그 순간 held-out 이 아니게 된다. CI 가 지키는 것은 라벨
무결성과 개발셋과의 분리뿐이다.

### v0.2 는 소진되었다

v0.2 의 오답을 읽고 엔진 결함 네 가지를 특정했고, 그 수정을 진행한다. 그 순간부터
v0.2 는 held-out 이 아니라 **개발셋**이다. 이후 v0.2 숫자는 회귀 확인용이지
일반화 성능이 아니다. 파일은 지우지 않고 그대로 둔다 — 지우면 과거 측정이
무엇을 잰 것인지 확인할 수 없게 된다.

---

# Fraud Holdout Set v0.3

`fraud_holdout_v0.3.jsonl`, 60건(사기 31 · 정상 29). ID 는 `fh-101`~`fh-160`.
로더는 `load_holdout_cases(HOLDOUT_V0_3_PATH)` 이며 **기본 경로가 없다.** "held-out
셋" 이라는 이름으로 부를 수 있게 두면 어느 버전을 쟀는지 기록에 남지 않는다.

## 이 셋이 v0.2 보다 약한 주장을 한다

v0.2 는 결함을 모르는 상태에서 썼다. v0.3 은 **결함 네 가지를 이미 특정하고 수정
방향까지 정한 뒤에** 썼다. 그러므로 이것은 일반화 성능 추정치가 아니라 **표적
회귀 측정**이다. 수정이 의도한 자리에서 작동하는지, 그리고 **의도하지 않은 자리를
망가뜨리지 않는지**를 본다. 후자가 이 셋의 진짜 일이다.

지킨 것은 순서다. 60건을 모두 쓰고 커밋해 동결한 **뒤에** 엔진을 건드린다.
커밋 이력이 그 순서를 증거로 남긴다. 수정 전 기준선도 동결 커밋에 함께 넣어,
"고치고 나서 기준선을 정했다" 는 의심이 성립할 수 없게 했다.

## 무엇을 겨냥해 넣었나

아홉 묶음이고, 각 묶음이 짝을 이룬다. 사기와 정상이 **같은 어휘**를 쓰되 요구의
유무·방향·목적어만 다르게 만들었다. 어휘 목록으로는 절대 가를 수 없는 짝이다.

| 묶음 | 사례 | 무엇을 재나 |
|---|---|---|
| A | `fh-101`~`110` 사기 10 | 기관 자칭 + 위험 요구 |
| B | `fh-111`~`120` 정상 10 | **A 와 같은 기관**의 정상 안내. 요구 없음 |
| C | `fh-121`~`128` 정상 8 | 사용자가 자기 행동을 설명하는 문장 |
| D | `fh-129`~`133` 정상 5 | 전달·다시 보내 + 목적어가 돈이 아님 |
| E | `fh-134`~`138` 사기 5 | **같은 동사** + 목적어가 돈 |
| F | `fh-139`~`143` 사기 5 | 선입금·수수료 요구 (`advance_fee_demand`) |
| G | `fh-144`~`148` 정상 5 | 예방 안내문. 사기 문자와 어휘가 거의 같다 |
| H | `fh-149`~`159` 사기 11 | 나머지 유형과 조합 |
| I | `fh-160` 정상 1 | 정책자금 정상 안내 |

A/B 가 기관 어휘 결함, D/E 가 목적어 결함, C/G 가 요구 맥락 결함을 겨냥한다.
`fh-136`(옮기→옮겨)이 어미 결함을, `fh-138`("넘겨")이 **어휘 공백**을 겨냥한다 —
후자는 맞히지 못할 것을 알고 넣었다. 라벨을 규칙 어휘에 맞추면 그 셋은 아무것도
재지 못한다.

`fh-128`("앱을 다시 설치했습니다 … 인증서는 어떻게 옮기나요")과 `fh-148`(부정문
안의 OTP·비밀번호 + 뒷문장의 명령형)은 **수정이 과했는지**를 보는 자리다.
어미만 넓히거나 요구 판단을 문장 단위가 아니라 메시지 단위로 하면 여기서 걸린다.

## 라벨 규칙

v0.2 규칙을 그대로 쓰고 두 가지를 더했다. 모두 **측정 전에** 정했다.

- `advance_fee_demand` 는 "먼저 보내면 돌려준다·처리해 준다" 구조에 붙인다.
  받은 돈을 넘기라는 요구는 `money_mule_transfer` 그대로다. `fh-152`(검찰 사칭 +
  안전계좌 송금)에는 **붙이지 않았다.** 대가를 약속하지 않기 때문이다. 유형을
  하나 새로 만들었다고 모든 송금 요구에 붙이면 그 유형은 이름값을 잃는다.
- `required_action_codes` 는 **상태와 필수 신호에서 실제로 도출되는 것만** 적는다.
  `_state_policy_accuracy` 가 부분집합을 요구하므로 한 개만 더 적어도 오답이 된다.
  60건 전부를 `STATE_ACTIONS ∪ SIGNAL_ACTIONS` 에 대조해 확인했다.

## 알면서 남긴 불일치

`expected_min_risk` 는 위 규칙("명시적 요구가 있으면 high")을 사람 기준으로
적용했다. 그런데 현재 등급 정책은 **사칭 + 요구** 조합을 두 쌍
(`authority+money_transfer`, `loan+account_access`)만 high 로 올린다. 그래서 10건
(`fh-102`·`103`·`104`·`105`·`110`·`139`·`140`·`141`·`157`·`159`)은 완벽히 탐지해도
medium 에 걸린다.

**이번 수정에서 등급 정책은 건드리지 않는다.** 탐지 수정과 정책 수정을 같이 하면
숫자가 움직였을 때 어느 쪽 덕인지 말할 수 없다. 이 10건은 수정 전후 모두 등급
불일치로 남을 것이고, 그렇게 남는 것 자체가 이번 수정이 탐지 전용이었다는 증거다.
정책 확장은 별도 항목으로 `docs/10-mvp-backlog.md` 에 있다.

## 출처와 권리

FinShield 팀이 2026-08-20 직접 작성한 합성 문장이다. 실제 피해자 메시지, 개인정보,
외부 데이터셋 문장을 복사하지 않았다. 기관명은 실재하지만 문장은 전부 창작이며,
**실제 그 기관이 보낸 안내가 아니다.** 링크는 접속되지 않는 형태(사설 IP 대역이
아닌 예시 주소·단축 링크 형식)로 적었고, 서버는 이 문자열을 절대 열지 않는다.

## 이용 한계

합성이고, 60건이며, 한국어 문자·메신저 형식에 한정된다. 저자가 엔진 어휘를 알고
있고 결함까지 알고 썼다. 그래서 이 숫자는 **실서비스 탐지 성능이 아니고**,
v0.2 와 직접 비교해서도 안 된다(구성이 다르다). 읽는 법은 하나뿐이다 —
**같은 v0.3 에 대한 수정 전과 수정 후의 차이.**

CI 는 v0.2 와 마찬가지로 이 셋에 성능 기준선을 걸지 않는다.

> **소진됨.** 위 「수정 전 / 수정 후」가 이 셋의 유일한 깨끗한 측정이다.
> 그 차이를 읽는 순간 이 셋의 오답을 알게 됐으므로 이후 v0.3 숫자는 회귀
> 확인용이다.
>
> **2026-08-21 회귀 확인.** v0.4 taxonomy 확장 뒤 다시 쟀고 **수치가 한 자리도
> 바뀌지 않았다.** 새 유형 행이 지지 0 으로 추가된 것이 전부다. 이번 변경은
> 이 셋을 보고 만들지 않았다.


---

# Fraud Holdout Set v0.4

`fraud_holdout_v0.4.jsonl`, 60건(사기 36 · 정상 24). ID 는 `fh-201`~`fh-260`.
로더는 `load_holdout_cases(HOLDOUT_V0_4_PATH)` 이며 v0.2·v0.3 과 마찬가지로
**기본 경로가 없다.**

## 이 셋은 "새로 생긴 능력" 을 잰다

v0.3 은 이미 있던 탐지의 결함을 겨냥한 표적 회귀 셋이었다. v0.4 는 다르다.
`investment_scheme` 과 `acquaintance_impersonation` 은 이 셋을 얼릴 때
**taxonomy 에 이름만 있고 아무것도 그 유형을 내지 못하는 상태**였다. 그래서 구현
전 baseline 의 두 유형 f1 이 정확히 0.000 이다. 그 0 이 사후 주장이 아니라 커밋에
남아 있다는 것이 이 셋의 요점이다.

| 커밋 | 내용 |
|---|---|
| `30563dd` | taxonomy 에 두 유형 선언. 엔진 변경 없음 |
| `5ab2bad` | **이 셋 동결 + 구현 전 baseline** |
| `6951b2e` | 엔진 구현 |

## 무엇을 겨냥해 넣었나

| 묶음 | 사례 | 무엇을 재나 |
|---|---|---|
| A | `fh-201`~`216` | 투자 권유 16건 (그중 13건이 `investment_scheme`) |
| B | `fh-217`~`227` | 지인·가족 사칭 11건 |
| C | `fh-228` | 정상 1건. 사칭 피해를 **겪은 사람의 자기보고** |
| D | `fh-229`~`238` | 기존 7개 유형 회귀 10건 |
| E | `fh-239`~`260` | 정상 22건. **절반이 새 어휘를 일부러 달고 있다** |

E 가 이 셋의 진짜 일이다. 투자 어휘는 지금까지 잰 어떤 어휘보다 정상 은행 문자와
겹친다. "원금 보장" 은 투자 권유에서 자본시장법이 금지한 거짓이고 예금 안내문에서는
사실이다. 그래서 정상 24건 중 12건에 원금·수익률·투자·단톡방·리딩방·폰·번호를
심었고, **그 성질을 파일에 대한 신뢰가 아니라 테스트로 고정했다**
(`test_holdout_v0_4_prices_the_new_vocabulary_against_normal_sentences`).

특히 노린 자리:

- `fh-240` "예금자보호법에 따라 이 상품은 원금이 보장됩니다" — 제도 근거가 있는 사실
- `fh-243` "원금이 보장되지 않습니다" — 투자설명서의 부정형
- `fh-244` 회사 동호회 단톡방 초대 링크 — 방 이름은 있으나 투자와 무관
- `fh-245`·`fh-260` 금융감독원 예방 안내문 — 사기 문자와 어휘가 거의 같다
- `fh-249` 동생 폰 분실 자기보고 — 연락 수단 변경 어휘가 정상 문맥에 있음
- `fh-211` "리딩방에서 알려준 계좌로 보냈는데" — 피해자 자기보고

`fh-211`·`fh-228`·`fh-249` 가 이 셋에서 가장 중요한 정상 사례다. 이 제품의 사용자는
**이미 당한 뒤에** 오는 경우가 많고, 그 사람이 자기가 겪은 일을 설명하는 문장을
사기로 판정하면 제품이 그 사람을 두 번 다치게 한다.

## 어휘 설계를 이 셋보다 먼저 정했다

`private_channel_invite` 를 맨 명사가 아니라 초대·입장 구절로만 잡기로 한 결정은
이 파일을 쓰기 **전에** 내렸고, 그래서 `fh-211` 이 통과한 것은 우연이 아니다.
반대로 결정을 나중에 했다면 이 셋은 그 결정을 검증하지 못했을 것이다.

## 라벨 규칙

v0.3 규칙을 그대로 쓰고 두 가지를 더했다. 모두 측정 전에 정했다.

- `investment_scheme` 은 **수익을 약속하거나 폐쇄 채널로 유인하는 권유**에 붙인다.
  투자 이야기가 나온다는 이유만으로 붙이지 않는다. `fh-211`(피해자 자기보고)에는
  붙이지 않았다.
- `acquaintance_impersonation` 은 **자칭 관계 + 연락 수단이 바뀐 핑계**에 붙인다.
  "엄마야" 만 있고 핑계가 없으면 붙이지 않는다 — 진짜 가족도 그렇게 쓴다.

## 알면서 남긴 불일치

v0.3 의 등급 정책 공백(사칭·제안 + 요구를 두 쌍만 high 로 올림)은 이번에도
그대로다. 이번 변경은 **새 신호가 들어간 조합만** `HIGH_RISK_SIGNAL_COMBINATIONS`
에 추가했고, 기존 신호끼리의 조합은 손대지 않았다. 같이 고치면 v0.3 수치가 무엇
때문에 움직였는지 갈라낼 수 없기 때문이다.

## 출처와 권리

FinShield 팀이 2026-08-21 직접 작성한 합성 문장이다. 실제 피해자 메시지, 개인정보,
외부 데이터셋 문장을 복사하지 않았다. 기관명은 실재하지만 문장은 전부 창작이며,
**실제 그 기관이 보낸 안내가 아니다.** 링크는 접속되지 않는 형태로 적었고, 서버는
이 문자열을 절대 열지 않는다. v0.1·v0.2·v0.3 과 본문·ID 가 하나도 겹치지 않으며
그 사실은 테스트로 고정돼 있다.

## 이용 한계

합성이고, 60건이며, 한국어 문자·메신저 형식에 한정된다. 저자가 엔진 어휘를 알고
있다. 두 새 유형에 대해서는 **구현 전 f1 이 0.000 인 상태에서 동결했으므로 그
차이가 표적 회귀가 아니라 새 능력을 재지만**, 그래도 실서비스 탐지 성능은 아니다.
v0.2·v0.3 과 직접 비교해서도 안 된다(구성이 다르다).

CI 는 앞선 셋들과 마찬가지로 이 셋에 성능 기준선을 걸지 않는다.

> **2026-08-21 소진.** 이 셋이 찾아 준 결함 8건(미탐 5 · 오탐 3)은 `docs/32` 에
> 이름으로 적어 두었고, v0.5 를 얼린 뒤에 고쳤다. 재측정 결과 f1 0.9459 → 0.9863,
> 오탐 3건 → 1건, 미탐 1건 → **0건**. 필수 signal coverage 0.6786 → 0.9643 중
> 상당 부분은 아래 「이름 오류」 수정분이다.

> **필수 signal 코드 이름 오류 (2026-08-21 수정).** 이 셋의 11건이
> `required_signal_codes` 에 **내부 규칙 이름**을 적고 있었다
> (`account_access_request` 등). 응답은 `project_public_signals` 를 거쳐
> `account_access` 로 나가므로 그 조건은 어떤 엔진으로도 만족될 수 없었다. 응답이
> 싣는 이름으로 고쳤고, 검증기가 이제 실릴 수 없는 코드를 거부한다. 이진 지표·
> action coverage·정책 정확도는 이 필드를 읽지 않아 영향이 없다.


# Fraud Holdout Set v0.5

`fraud_holdout_v0.5.jsonl`, 72건(사기 44 · 정상 28). ID 는 `fh-301`~`fh-372`.
**held-out 이며 개발셋과 파일이 분리돼 있다.**

## 왜 또 얼렸나

v0.4 가 결함 8건을 찾아 줬다. 그 결함을 v0.4 를 보면서 고치고 다시 v0.4 로 재면,
그 셋은 성능이 아니라 **기억**을 재게 된다. 그래서 고치기 전에 v0.5 를 얼렸고,
수정 코드가 존재하기 전에 baseline 을 같은 커밋에 넣었다(`8930328`). 순서는 주장이
아니라 `git log` 로 확인된다.

## 무엇을 겨냥해 만들었나

이번 라운드가 계획한 **네 가지 수정** 각각을 겨냥한다. 사례는 수정 방식이 아니라
**결함의 부류**를 보고 썼다 — 구현을 정하기 전에 썼기 때문이다.

| 그룹 | 사례 | 노린 것 |
|---|---|---|
| A | `fh-301`~`fh-312` (양성 12) | 활용형·조사 차이("폰 고장", "번호 바꿨어", "3배 확정") |
| B | `fh-313`~`fh-320` (양성 8) | 반말 요구("넘겨 줘", "깔아 놔", "켜 놔") |
| C | `fh-321`~`fh-326` (양성 6) | 절 범위를 좁히면 **죽는** 진짜 사기. 요구와 대상이 다른 절에 있다 |
| C | `fh-327`~`fh-332` (양성 6) | "하지 않" 을 조건절로 쓰는 협박문. 6개 유형에 흩어 놓았다 |
| D | `fh-333`~`fh-344` (양성 12) | 9개 유형 전체 회귀 |
| E | `fh-345`~`fh-372` (정상 28) | **네 가지 수정을 정확히 깨도록 고른 문장** |

## 정상 28건이 이 셋의 본체다

이번 수정 네 가지는 전부 **재현율을 오탐으로 사는** 종류다. 그래서 정상 사례를
39% 로 올리고, 각 수정이 넘어질 자리를 하나씩 배치했다. 그 성질은 테스트로 고정돼
있다(`test_holdout_v0_5_prices_each_planned_fix_against_normal_sentences`).

- `fh-345`~`fh-348` — 아직 목록에 없는 부정 어간의 예방 안내문("드리지 않",
  "보장하지 않", "안내하지 않", "추천하지 않/운영하지 않")
- `fh-350`~`fh-352` — 반말 요구가 정상으로 오는 경우
- `fh-353`·`fh-354`·`fh-368` — 폰·번호 어휘 충돌("폰 배터리", "번호가 바뀌어서")
- `fh-356`~`fh-361` — 절 범위 오탐 표적. 요구와 대상이 다른 절에 흩어져 있다
- `fh-362`~`fh-365` — 활용형 확대 가드("만기일이 확정", "설명회 입장", "깔았는데",
  "회사 단톡방에")
- `fh-367` — 피해자 자기보고

`fh-327`~`fh-332` 양성 6건이 서로 다른 6개 유형에 걸쳐 "하지 않" 을 품고 있는 것도
같은 목적이다. `PREVENTION_STATEMENT_MARKERS` 에 맨 "하지 않" 을 넣는 실수를 하면
한 유형이 시끄러워지는 게 아니라 **여러 유형이 동시에 무너지는** 모양으로 나타난다.

## 라벨 규칙

v0.4 규칙을 그대로 쓴다. 새 유형도, 새 액션도 없다. 이번 라운드는 **taxonomy 를
건드리지 않는 순수 탐지 수정**이고, 그래서 유형 표는 v0.4 와 같은 9종이다.

`required_signal_codes` 는 **응답에 실제로 실리는 이름**으로 적는다. 처음에는
내부 규칙 이름으로 적었고 24건이 그렇게 틀려 있었다(`docs/32` 참조). 검증기가 이제
실릴 수 없는 코드를 거부한다.

## 출처와 권리

FinShield 팀이 2026-08-21 직접 작성한 합성 문장이다. 실제 피해자 메시지, 개인정보,
외부 데이터셋 문장을 복사하지 않았다. 기관명은 실재하지만 문장은 전부 창작이며,
**실제 그 기관이 보낸 안내가 아니다.** 링크는 접속되지 않는 형태로 적었고, 서버는
이 문자열을 절대 열지 않는다. v0.1~v0.4 와 본문·ID 가 하나도 겹치지 않으며 그
사실은 테스트로 고정돼 있다.

## 이용 한계

합성이고, 72건이며, 한국어 문자·메신저 형식에 한정된다. 저자가 엔진 어휘를 알고
있다. **수정 전 오탐률 0.3929 는 현장 추정치가 아니라 가격표다** — 정상 28건을
계획한 수정이 깨질 자리에 일부러 몰아 놓았기 때문이다. v0.2·v0.3·v0.4 와 직접
비교해서도 안 된다(구성이 다르다).

CI 는 앞선 셋들과 마찬가지로 이 셋에 성능 기준선을 걸지 않는다.

> **2026-08-21 소진.** 이 셋이 찾은 결함 중 고치지 않은 것(`fh-308` 미탐,
> `fh-352`·`fh-354`·`fh-358`·`fh-360`·`fh-361` 오탐)은 `docs/32` 에 이름과 이유로
> 적어 두었다. 고치려면 v0.6 을 먼저 얼려야 한다.
>
> **2026-08-22 추가.** v0.6 을 얼린 뒤 그 세 결함을 고쳤고, 여기서 다시 쟀다.
> f1 0.9348 → 0.9670, 오탐률 0.1786 → 0.1071, 필수 신호 coverage 0.9836 →
> 1.0000. 위에 적어 둔 6건 중 `fh-308`·`fh-360`·`fh-361` 이 사라졌다. **이 수치는
> 소진된 셋의 회귀 확인이지 성능 주장이 아니다.**


# Fraud Holdout Set v0.6

`fraud_holdout_v0.6.jsonl`, 72건(사기 36 · 정상 36). ID 는 `fh-401`~`fh-472`.
**held-out 이며 개발셋과 파일이 분리돼 있다.**

## 이 셋은 앞선 넷과 방향이 반대다

v0.2 부터 v0.5 까지 잰 수정은 전부 **넓히는** 것이었다. 어휘를 늘리고, 활용형을
추가하고, 반말 요구 표지를 넣었다. 넓히기의 값은 정상 문장에서 치러지므로 그 셋들의
안전장치는 **부정 사례를 충분히 담는 것**이었다.

이번 라운드가 계획한 세 가지 중 둘은 **좁히는** 것이다 — 기관 자칭 게이트에 민감
요구 조건을 걸고, 공식 창구를 가리키는 절을 요구에서 뺀다. **좁히기의 값은 정상
문장이 아니라 사기 쪽에서 치러진다.** 오탐률이 내려가는데 그 대가가 표에 안 보이면,
그 표는 개선을 보여 주는 것이 아니라 개선을 숨기는 것이다.

그래서 이 셋의 안전장치는 부정 사례 수가 아니라 **좁히기가 끊을 수 있는 자리에 놓인
양성 사례 수**다. 정상과 사기를 36 대 36 으로 놓고, 양쪽에 같은 어휘를 쓰게 했다.
그 성질은 테스트로 고정돼 있다
(`test_holdout_v0_6_prices_two_narrowings_with_fraud_not_with_normal_text`) —
창구를 가리키는 말이 든 사기 4건 이상, 기관명이 든 사기 6건 이상.

## 무엇을 겨냥해 만들었나

| 그룹 | 사례 | 노린 것 |
|---|---|---|
| A | `fh-401`~`fh-406` (양성 6) | 폐쇄 채널 초대 **+ 매매 맥락**. 맥락 조건을 잘못 걸면 여기가 죽는다 |
| A' | `fh-407`~`fh-413` (정상 7) | 같은 방 이름을 쓰는 학부모·동아리·워크샵 공지, 정상 매매 어휘 |
| B | `fh-414`~`fh-421` (양성 8) | 기관 자칭 **+ 민감 요구**. 게이트를 너무 좁히면 여기가 죽는다 |
| B' | `fh-422`~`fh-434` (정상 13) | 기관 자칭 + **무해한** 요구, 공식 창구로 내보내는 안내문 |
| C | `fh-435`~`fh-438` (양성 4) | **창구 안내를 위장으로 쓰는 사기.** 좁히기의 값이 정확히 여기서 나온다 |
| D | `fh-439`~`fh-456` (양성 18) | 9개 유형 전체 회귀 |
| D' | `fh-457`~`fh-472` (정상 16) | 회귀 가드 — 피해자 자기보고, 예방 안내, 정상 계좌 변경 통지 |

C 그룹이 이 셋의 핵심이다. "고객센터로 문의 가능하지만 대기가 깁니다. 빠른 처리를
위해…"(`fh-436`)처럼, 공식 창구를 **언급만 하고** 실제로는 자기 쪽으로 끌어가는
문장이다. 창구 안내 규칙을 절 단위가 아니라 메시지 단위로 만들면 이 4건이 통째로
빠진다.

## 라벨 규칙

v0.5 규칙을 그대로 쓴다. 새 유형도, 새 액션도 없다 — 이번 라운드는 **taxonomy 를
건드리지 않는 순수 탐지 수정**이고 유형 표는 v0.4 이후와 같은 9종이다.
`required_signal_codes` 는 응답에 실제로 실리는 이름으로 적고, 검증기가 실릴 수 없는
코드를 거부한다.

## 알면서 남긴 것

`fh-472`(동호회 회비 계좌번호 공지)는 계좌번호와 입금 요구가 **정말로** 있다. 사적
모임 회비를 사기와 가르려면 이 서비스 범위 밖의 관계 정보가 필요하므로, 얼릴 때부터
범위 밖으로 표시해 두었다.

## 출처와 권리

FinShield 팀이 2026-08-22 직접 작성한 합성 문장이다. 실제 피해자 메시지, 개인정보,
외부 데이터셋 문장을 복사하지 않았다. 기관명은 실재하지만 문장은 전부 창작이며,
**실제 그 기관이 보낸 안내가 아니다.** 링크는 접속되지 않는 형태로 적었고, 서버는
이 문자열을 절대 열지 않는다. v0.1~v0.5 와 본문·ID 가 하나도 겹치지 않으며 그 사실은
테스트로 고정돼 있다.

## 이용 한계

합성이고, 72건이며, 한국어 문자·메신저 형식에 한정된다. 저자가 엔진 어휘를 알고
있다. **수정 후 오탐률 0.0278 을 현장 추정치로 읽으면 안 된다** — 정상 36건이 좁히기
수정이 지워야 할 자리에 몰려 있어서, 그 수정이 성공하면 구조적으로 낮게 나온다.
같은 이유로 v0.2~v0.5 의 오탐률과 나란히 두고 읽어도 안 된다(구성이 다르다).

CI 는 앞선 셋들과 마찬가지로 이 셋에 성능 기준선을 걸지 않는다.

> **2026-08-22 소진.** 이 셋이 찾은 결함 중 고치지 않은 것(`fh-438`·`fh-446`·
> `fh-454` 미탐, `fh-472` 오탐, 그리고 정책 등급이 모자란 `fh-415`·`fh-416`·
> `fh-418`~`fh-421`)은 `docs/32` 에 이름과 이유로 적어 두었다. 고치려면 v0.7 을
> 먼저 얼려야 한다.

> **2026-08-22 추가.** v0.7 을 얼린 뒤 그 결함들을 고쳤고, 여기서 다시 쟀다.
> `fh-446`(재전달 어휘)과 `fh-454`(고립 요구에 유형이 없던 것)가 잡히면서 미탐이
> 3건 → 1건, f1 0.9429 → 0.9722 가 됐다. 정책 6건은 조합 확장으로 전부 채워져
> 상태 정책 정확도가 0.9028 → 0.9861 이다. `fh-438`(좁히기의 값)과 `fh-472`(서비스
> 범위 밖)는 그대로 남았다. **v0.6 을 보고 고친 것이 아니라 v0.7 을 얼린 뒤에
> 고쳤으므로, 위 수치는 기억이 아니라 회귀 확인이다.**

# Fraud Holdout Set v0.7

`fraud_holdout_v0.7.jsonl`, 72건(사기 36 · 정상 36). ID 는 `fh-501`~`fh-572`.

## 왜 또 새 셋인가

v0.6 이 이름으로 적어 둔 결함 셋 중 하나가 `HIGH_RISK_SIGNAL_COMBINATIONS` 확장이다.
그 수정은 신호 구성이 같은 메시지의 **등급만** 올린다.

그것을 재려고 지표를 보니 `_scenario_policy_accuracy` 가 `expected_min_risk` 에 대해
`>=` 만 보고 있었다. **모든 메시지에 `high` 를 찍는 엔진이 이 지표에서 1.0 을 받는다.**
넓히기(v0.2~v0.5)와 좁히기(v0.6)는 각각 값을 치를 자리가 있었는데, **올리기만 값을
치를 자리가 없었다.**

그래서 이 셋은 사례를 쓰기 전에 지표부터 고쳤다.

## 이 셋이 새로 선언하는 것: 등급 천장

`expected_max_risk` 를 **정상 문장 36건 전부**에 건다. 사기 문장에는 걸지 않는다 —
사기를 필요 이상으로 높게 매기는 것은 결함이 아니라 신중함이고, 거기에 천장을 씌우면
신중함에 벌점을 주게 된다.

`risk_ceiling_accuracy` 는 천장을 선언한 사례에서만 재고, 하나도 없으면 `1.0` 이
아니라 **`None`** 을 돌려준다. 분모 0을 만점으로 적는 것은 "재지 않았다" 를
"완벽했다" 로 바꿔 적는 일이다.

`expected_max_risk` 가 `None` 이면 `normalized_dataset_sha256` 계산에서 뺀다.
그러지 않으면 이 필드를 추가하는 것만으로 **기존 여섯 셋의 신원이 전부 바뀌고**, 유료
LLM 판정이 붙어 있는 개발셋의 출처 연결까지 끊긴다. `held_out` 을 이미 같은 이유로
빼고 있다 — **선언하지 않은 라벨은 라벨이 아니다.**

## 구성

| 그룹 | 사례 | 노린 것 |
|---|---|---|
| A | `fh-501`~`fh-510` (양성 10) | 기관 사칭 + 넘겨주기 요구 |
| A' | `fh-511`~`fh-518` (정상 8) | 같은 기관·계좌·카드 어휘의 정상 안내 |
| B | `fh-519`~`fh-526` (양성 8) | 고립·확인 차단 요구 |
| B' | `fh-527`~`fh-536` (정상 10) | 대외비·NDA·생일 파티 등 평범한 비밀 유지 |
| C | `fh-537`~`fh-544` (양성 8) | 들어온 돈 재전달 요구의 여러 어형 |
| C' | `fh-545`~`fh-552` (정상 8) | 자기 계좌 간 이동·정상 정산·착오송금 예방 안내 |
| D | `fh-553`~`fh-562` (양성 10) | 9개 유형 회귀 |
| D' | `fh-563`~`fh-572` (정상 10) | 회귀 가드 + 천장 |

**그룹 A 를 전부 `received_only` 로 뒀다.** 상태 하한이 등급을 대신 올려 버리면 정책
수정이 무슨 일을 했는지 보이지 않는다. 이 조건은 통합 테스트가 센다
(`test_holdout_v0_7_prices_the_grade_raising_fix_where_it_can_overshoot`).

`isolation_coercion` 은 엔진이 그것을 낼 수 있기 **전에** 평가 taxonomy 에 선언했다.
v0.4 가 투자·지인 유형에 쓴 것과 같은 순서다.

## 출처와 권리

FinShield 팀이 2026-08-22 직접 작성한 합성 문장이다. 실제 피해자 메시지, 개인정보,
외부 데이터셋 문장을 복사하지 않았다. 기관명은 실재하지만 문장은 전부 창작이며,
**실제 그 기관이 보낸 안내가 아니다.** 링크는 접속되지 않는 형태로 적었고, 서버는
이 문자열을 절대 열지 않는다. v0.1~v0.6 과 본문·ID 가 하나도 겹치지 않으며 그 사실은
테스트로 고정돼 있다.

## 이용 한계

합성이고, 72건이며, 한국어 문자·메신저 형식에 한정된다. 저자가 엔진 어휘를 알고
있다. **수정 후 오탐률 0.0000 과 천장 정확도 1.0000 을 현장 추정치로 읽으면 안 된다** —
정상 36건이 이번 수정이 넘지 말아야 할 자리에 몰려 있어서, 수정이 성공하면 구조적으로
만점이 나온다. 이 두 숫자는 "이번 수정이 정상 문장을 건드리지 않았다" 는 뜻이지
"정상 문장을 건드리는 일이 없다" 는 뜻이 아니다.

CI 는 앞선 셋들과 마찬가지로 이 셋에 성능 기준선을 걸지 않는다.

> **2026-08-22 소진.** 이 셋이 찾은 결함 중 고치지 않은 것(`fh-508`·`fh-523`·
> `fh-539`·`fh-542`·`fh-543`·`fh-561`·`fh-562` 미탐)은 `docs/32` 에 이름과 진단으로
> 적어 두었다. 고치려면 v0.8 을 먼저 얼려야 한다.

> **2026-08-23 추가.** v0.8 을 얼린 뒤 그 일곱 결함을 고쳤고, 여기서 다시 쟀다.
> f1 0.8923 → **1.0000**, 미탐 7 → **0**, 상태 정책 0.8750 → **0.9444**.
> **이 숫자는 성능 주장이 아니다** — 일곱 수정이 전부 이 셋이 기록한 미탐에서
> 나왔으므로 이 문장들은 수정의 **출처**이지 시험이 아니다. 회귀 확인으로만 읽는다.

# Fraud Holdout Set v0.8

`fraud_holdout_v0.8.jsonl`, 72건(사기 36 · 정상 36). ID 는 `fh-601`~`fh-672`.
정규화 SHA-256 `9f85da97ebd51e740f0171f65a6612b78c1d6ae76e08798f78a44012dbc74fed`.

## 왜 이 셋인가

v0.7 이 이름으로 적어 둔 결함 일곱 건은 **거의 전부 어휘를 넓히는 수정**이다. 넓히기의
값은 정상 문장에서 치러진다. 동결 시점 baseline 이 precision 1.0000 / FPR 0.0000
이므로 그 두 칸은 **올라갈 자리가 없고 내려갈 자리만 있다.**

그래서 정상 36건을 각 넓히기가 깨질 수 있는 자리에 하나씩 배치했다. 유일한
좁히기(`드리지 않` 을 보조용언 자리로 좁히는 것)의 값도 정상 쪽에서 낸다 — 그 억제기가
과잉이라 풀면, 풀린 만큼 **진짜 예방 안내문**이 사기로 올라간다.

## 구성

| 그룹 | 사례 | 노린 것 |
|---|---|---|
| A | `fh-601`~`fh-606` (양성 6) | 권한 위임 요구 |
| A' | `fh-607`~`fh-612` (정상 6) | 정상 권한·위임 — 세무 대리인, 법인 위임장, 부모 계좌 대리 조회 |
| B | `fh-613`~`fh-618` (양성 6) | 재전달 어형 |
| B' | `fh-619`~`fh-626` (정상 8) | 정상 돈 이동 — 자기 계좌 간 이체, 회비 정산, **금지 명령** |
| C | `fh-627`~`fh-631` (양성 5) | 고립 요구 |
| C' | `fh-632`~`fh-637` (정상 6) | **진짜 예방 안내문** — 좁히기가 값을 치를 자리 |
| D | `fh-638`~`fh-643` (양성 6) | 지검 사칭·선입금 |
| D' | `fh-644`~`fh-651` (정상 8) | 정상 기관·입금 |
| E | `fh-652`~`fh-664` (양성 13) | 10개 유형 회귀 |
| E' | `fh-665`~`fh-672` (정상 8) | 회귀 가드 |

**정상 36건 전부가 천장을 선언한다** — `fh-645`·`fh-646` 만 medium, 나머지 34건 low.
통합 테스트 셋이 이 배치를 센다: taxonomy·천장 규율
(`test_holdout_v0_8_covers_the_taxonomy_and_keeps_the_ceiling_discipline`), 다섯
넓히기가 각각 깨질 자리에 정상 문장이 있는지
(`test_holdout_v0_8_prices_five_widenings_where_each_one_can_break`), 좁히기가 양쪽에서
값을 내는지
(`test_holdout_v0_8_prices_the_prevention_marker_narrowing_on_both_sides`).

## 얼리기 전에 고친 라벨 두 개

- `fh-643` — `transferred_money` 상태를 아무도 쓰지 않아 `_validate_collection` 이
  거부했다. 상태를 바꾸고 본문을 그에 맞게 다시 썼다.
- `fh-660` — `money_transfer_request` 를 함께 요구했는데
  `project_public_signals` 는 `receive_and_forward_money` 가 켜지면 공개 응답에서 그
  이름을 **일부러 뺀다.** API 가 절대 내보내지 않는 이름을 요구하고 있었다.

**둘 다 측정 전이다.** 측정 후에 라벨을 고치는 것은 채점표를 고치는 일이다.

## 셋이 결함을 재현하는지 동결 전에 확인했다

그룹 C 초안이 "저와만 연락 주세요" 였는데 `주세요` 가 예방 억제를 풀어 버려서 baseline
에서 이미 잡혔다. 맨 명령형("저와만 연락하세요")으로 바꾸고 나서야 진짜 미탐이 됐다.
**결함을 재현하지 않는 사례는 수정의 값을 재지 못하고 만점만 하나 더 얹는다.**

## 출처와 권리

FinShield 팀이 2026-08-23 직접 작성한 합성 문장이다. 실제 피해자 메시지, 개인정보,
외부 데이터셋 문장을 복사하지 않았다. 기관명은 실재하지만 문장은 전부 창작이며,
**실제 그 기관이 보낸 안내가 아니다.** 링크는 접속되지 않는 형태로 적었고, 서버는
이 문자열을 절대 열지 않는다. v0.1~v0.7 과 본문·ID 가 하나도 겹치지 않으며 그 사실은
테스트로 고정돼 있다.

## 이용 한계

합성이고, 72건이며, 한국어 문자·메신저 형식에 한정된다. 저자가 엔진 어휘를 알고 있다.
**수정 후 accuracy 1.0000 을 현장 추정치로 읽으면 안 된다** — 72건 전부가 이번 일곱
수정이 건드릴 자리와 건드리면 안 될 자리에 몰려 있어서, 수정이 성공하면 구조적으로
만점이 나온다. 이 숫자는 "이번 수정이 겨냥한 것을 잡고 정상 문장을 건드리지 않았다"
는 뜻이지 "일반적인 사기 문자를 다 잡는다" 는 뜻이 아니다.

이진 판정이 만점이라는 것은 이 셋의 변별력이 **이름·행동·등급** 쪽으로만 남았다는
뜻이기도 하다. 다음 셋은 그쪽을 겨냥해 짜야 한다.

CI 는 앞선 셋들과 마찬가지로 이 셋에 성능 기준선을 걸지 않는다.

> **2026-08-23 소진.** 이 셋이 찾은 결함 중 고치지 않은 것(`fh-638`·`fh-657` 유형
> 과다, `fh-603`·`fh-627`~`fh-629`·`fh-631` action 누락, `fh-663` 등급 미달)은
> `docs/32` 에 이름과 진단으로 적어 두었다. 고치려면 v0.9 를 먼저 얼려야 한다.

# Fraud Holdout Set v0.9

`fraud_holdout_v0.9.jsonl`, 72건(사기 41 · 정상 31). ID 는 `fh-701`~`fh-772`.
정규화 SHA-256 `63ab06259bcdb5a1520300ce218566c3c974df6dfeb37420b505552bea54978f`.

## 왜 이 셋인가

v0.8 에서 이진 판정이 만점이 됐다. **사기/정상 사례를 더 넣는 것으로는 이 엔진에서
더 잴 것이 없다는 뜻이다.** 남은 변별력은 이름·행동·등급 쪽에만 있고, v0.8 이 이름으로
적어 둔 결함도 전부 그쪽이다.

이번 셋은 그중 **행동**을 정면으로 겨냥한다. 구체적으로 하나의 질문이다 —
*확인하라는 조언이 어디를 가리키는가.*

"공식 대표번호로 확인하세요" 는 확인할 기관이 지정됐을 때만 실행 가능한 조언이다.
이름을 대지 않은 상대에게 같은 말을 하면 사용자는 **존재하지 않는 창구**를 찾다가
결국 메시지에 적힌 번호로 건다. 등급이 한 칸 낮은 것보다 조용히 나쁘다.

## 값을 치를 자리가 양쪽이다

넓히기도 좁히기도 올리기도 아니라 **갈아 끼우기**다. 행동을 하나 더 붙이는 것이
아니라 다른 것으로 바꾼다. 그래서 양쪽 다 미끄러질 수 있다.

- 이름 없는 쪽으로 너무 돌면 **기관 사칭 14건**이 공식 창구를 잃는다.
- 자칭 쪽으로 너무 돌면 **지인 사칭 4건**이 v0.4 회귀가 된다.

두 자리를 같은 셋에 넣지 않으면 수정이 어느 쪽으로 미끄러졌는지 보이지 않는다.

## 재는 자를 먼저 고쳤다

동결 전에 `forbidden_action_codes` 와 `forbidden_action_avoidance` 를 넣었다
(`7369501`). `required_action_coverage` 는 `required ⊆ predicted` 만 세므로
**열두 행동을 전부 붙이는 엔진이 1.0 을 받는다.** v0.7 이 등급에서 발견한 것과
같은 모양이고, 이번 회차가 행동을 갈아 끼우므로 여기서 닫았다.

금지를 선언하지 않은 셋에서는 `1.0` 이 아니라 **`None`** 이 나간다. v0.1~v0.8 의
정규화 해시는 한 자리도 움직이지 않았다 — 선언하지 않은 라벨은 라벨이 아니다.

## 구성

| 그룹 | 사례 | 노린 것 |
|---|---|---|
| A | `fh-701`~`fh-710` (양성 10) | **이름을 대지 않은 상대** — 아는 창구 확인을 요구하고 공식 창구를 금지 |
| A' | `fh-711`~`fh-720` (양성 10) | **기관 자칭** — 공식 창구를 요구하고 아는 창구를 금지 |
| A'' | `fh-721`~`fh-724` (양성 4) | 지인 사칭 — v0.4 회귀 가드 |
| B | `fh-725`~`fh-729` (양성 5) | 위협 + 접근수단 요구인데 medium 에서 멈추는 자리 |
| C | `fh-730`~`fh-735` (양성 6) | 통장·카드 어휘의 **고발 3 vs 진짜 요구 3** |
| N1 | `fh-736`~`fh-747` (정상 12) | 기한이 급한 정상 안내 + 통장·카드 낱말 |
| N2 | `fh-748`~`fh-759` (정상 12) | 정상 권한 위임·정상 확인 안내 |
| E | `fh-760`~`fh-765` (양성 6) | 10개 유형 회귀 |
| E' | `fh-766`~`fh-772` (정상 7) | 회귀 가드 |

**정상 31건 전부가 천장을 선언하고 전부 `low` 다.** 금지 행동은 41건이 선언한다
(공식 창구 금지 27 · 아는 창구 금지 14). 통합 테스트 셋이 이 배치를 센다 —
`test_holdout_v0_9_covers_the_taxonomy_and_keeps_the_ceiling_discipline`,
`test_holdout_v0_9_pairs_every_demand_with_and_without_a_named_institution`,
`test_holdout_v0_9_prices_the_grade_raise_and_the_accusation_narrowing`.

## 셋이 결함을 재현하는지 동결 전에 확인했다

72건을 라벨 없이 먼저 엔진에 던졌고, **여덟 건이 겨냥한 결함을 재현하지 않아 다시
썼다.** 대부분은 초안 문장이 엔진 어휘를 빗나간 경우다 — `가족분들께 알리지`
(어휘는 `가족에게도 알리`), `오늘 자정까지`(긴급 어휘는 `오늘까지`), `옮기셔야`
(요구 어미는 `이체하셔`), 그리고 어휘에 없는 기관명 둘(`예금보험공사`·`서민금융진흥원`).

`fh-712` 는 다른 이유로 고쳤다. 고립 요구만으로는 `지검` 자칭이 켜지지 않는다 —
조건부 자칭 어휘는 **민감 요구**를 기다리는데 고립 요구가 그 목록에 없다. 사례에
계좌 권한 요구를 더해 자칭이 켜지게 했다. **조건부 기관 자칭이 고립 요구만으로는
안 켜진다**는 사실 자체는 이 셋이 재지 않는다(아래 참조).

**결함을 재현하지 않는 사례는 수정의 값을 재지 못하고 만점만 하나 더 얹는다.**

## 이 셋이 동결 시점에 이미 기록한 오탐 둘

정상 31건 중 둘이 동결 시점에 사기로 올라가 있다. **고치지 않고 얼렸다.**

- `fh-741` "인증번호는 저희를 포함해 누구에게도 알려 주시면 안 됩니다."
- `fh-767` "설치는 공식 스토어에서만 진행해 주세요."

둘 다 같은 모양이다 — **위험한 행동을 제한하기 위해 그 행동을 입에 올리는 안내문.**
v0.8 이 `PROHIBITION_ENDINGS` 로 `-지 마` 꼴을 닫았는데 `주시면 안 됩니다`·
`…에서만` 은 그 자리에 없다. 이번 회차 세 수정과 무관하므로 수정 후에도 오탐으로
남고, 그래서 이 셋의 precision 은 앞뒤 모두 0.9535 다.

## 이 셋이 재되 이번 회차에 고치지 않는 것

- `fh-729` — `1시간 안에` 가 긴급 어휘에 없다. 이 한 건이 signal coverage 0.9859 의
  전부이고, 등급도 그래서 오르지 않는다. **한 사례로 두 구멍을 동시에 적어 둔다.**
- 조건부 기관 자칭(`지검`·`공단`·`은행`)이 **고립 요구만으로는 켜지지 않는다.**
  사례로 만들지 않았으므로 어느 셋도 이것을 재고 있지 않다.

## 출처와 권리

FinShield 팀이 2026-08-23 직접 작성한 합성 문장이다. 실제 피해자 메시지, 개인정보,
외부 데이터셋 문장을 복사하지 않았다. 기관명은 실재하지만 문장은 전부 창작이며,
**실제 그 기관이 보낸 안내가 아니다.** 링크는 접속되지 않는 형태로 적었고, 서버는
이 문자열을 절대 열지 않는다. v0.1~v0.8 과 본문·ID 가 하나도 겹치지 않으며 그 사실은
테스트로 고정돼 있다.

## 이용 한계

합성이고, 72건이며, 한국어 문자·메신저 형식에 한정된다. 저자가 엔진 어휘를 알고 있다.
이진 판정 f1 0.9762 는 **성능이 아니라 배경**이다 — 이 셋은 이진 판정을 겨냥해 짜지
않았고, 사기 41건은 전부 동결 시점에 이미 잡히고 있었다. 읽을 칸은
`forbidden_action_avoidance`·`required_action_coverage`·`scenario_policy_accuracy`
세 개다.

CI 는 앞선 셋들과 마찬가지로 이 셋에 성능 기준선을 걸지 않는다.

> **2026-08-24 소진.** 이 셋이 동결 시점에 기록한 오탐 둘(`fh-741`·`fh-767`)은 v1.0
> 회차가 갚았고, 그 뒤 이 셋은 이진 판정·등급 천장 모두 만점이다. 남은 결함
> (`fh-762`·`fh-764`·`fh-765` 라벨 재검토)은 `docs/32` 에 이름으로 적어 두었다.
> **v1.0 이 이 셋의 문장 둘(`fh-741`·`fh-767`)을 그대로 물려받았다가 뺐다** —
> 얼린 셋의 문장은 다음 셋에 다시 등장하지 않는다.

# Fraud Holdout Set v1.0

`fraud_holdout_v1.0.jsonl`, 70건(사기 38 · 정상 32). ID 는 `fh-801`~`fh-872` 이고
`fh-839`·`fh-846` 은 결번이다(아래 "무결성 수리" 참조).
정규화 SHA-256 `ba90db9dbae7d7f578b58405aa4b27bbb22f5e993d2146fcaf4e266d091a9c19`.

## 왜 이 셋인가

**새로 상상한 사기 어형이 하나도 없다.** 앞선 회차들이 "다음 회차 몫" 이라고 이름만
적어 둔 결함을 재기 위해 얼린 셋이다. v0.9 devlog §9 의 목록이 그대로 다섯 그룹이 됐다.

| 앞선 회차가 적어 둔 것 | v1.0 그룹 |
|---|---|
| `PROHIBITION_ENDINGS` 가 못 받는 어형 (`fh-741`·`fh-767`) | A · A' |
| `…에서만` 채널 제한의 양면 (`fh-767` 을 파다 나온 자리) | B · B' |
| 긴급 어휘가 여섯 개뿐 (`fh-729`) | C · C' |
| `대환 대출`(띄어쓰기)이 어휘에 없다 | D · D' |
| 조건부 자칭이 고립 요구만으로는 안 켜진다 (v0.9 `fh-712`) | E · E' |

적어 둔 결함을 갚을 수 있다는 것 자체가 이 셋이 재는 값이다.

## 값을 치를 자리가 한쪽뿐이다

다섯 수정이 전부 **넓히기**다. 넓히기는 정상 문장 쪽으로만 미끄러지므로, 이 셋은
사기보다 **정상을 세게** 짰다. 정상 32건 전부가 등급 천장을 선언하고 전부 `low` 이며,
그중 스물여섯 건이 **넓히려는 바로 그 어형을 쓰는 정상 문장**이다.

```
"인증번호는 어떤 경우에도 알려 주시면 안 됩니다"   ← 넓히려는 금지 어형 (정상)
"창구 방문은 하시면 안 되고 사본을 보내 주셔야 합니다" ← 같은 어형 (사기)
```

같은 어형을 양쪽에 두지 않으면 수정이 어디까지 갔는지 보이지 않는다.

## 구성

| 그룹 | 사례 | 노린 것 |
|---|---|---|
| A | `fh-801`~`fh-806` (사기 6) | 우언적 금지로 **아는 창구를 막고** 그 뒤에서 요구 |
| B | `fh-807`~`fh-812` (사기 6) | `…에서만` 으로 **메시지 안의 경로**로 제한 |
| C | `fh-813`~`fh-818` (사기 6) | 기한을 숫자로 적은 재촉 + 접근수단 요구 |
| D | `fh-819`~`fh-821` (사기 3) | 띄어 쓴 `대환 대출` |
| E | `fh-822`~`fh-824` (사기 3) | 기관 자칭 + **고립 요구 하나만** |
| F | `fh-825`~`fh-838` (사기 14) | 앞선 아홉 회차 회귀 가드 (7개 상태 전부) |
| A' | `fh-840`~`fh-845` (정상 6) | 같은 금지 어형을 쓰는 **예방 안내문** |
| B' | `fh-847`~`fh-852` (정상 6) | 같은 제한 어형으로 **아는 창구**를 가리키는 공지 |
| C' | `fh-853`~`fh-859` (정상 7) | 같은 시간 표현을 쓰는 청구서·점검·배송 안내 |
| D' | `fh-860`~`fh-863` (정상 4) | 은행이 안내하는 진짜 대환 대출 |
| E' | `fh-864`~`fh-866` (정상 3) | 기관 자칭 + 대외비·비밀유지 |
| F' | `fh-867`~`fh-872` (정상 6) | 일반 정상 |

10개 유형·7개 상태·3개 persona 를 덮고 금지 행동은 13건이 선언한다. 통합 테스트가
이 배치를 센다 — `test_holdout_v1_0_covers_the_taxonomy_and_keeps_the_ceiling_discipline`,
`test_holdout_v1_0_prices_five_widenings_against_the_normal_form_they_touch`,
`test_holdout_v1_0_declares_a_ceiling_on_every_normal_and_names_what_it_forbids`.

C' 에는 **일부러 빗나가게 쓴 두 건**이 있다. `fh-853` 은 "내일 오후 3시"(더한 어휘는
`내일까지`), `fh-855` 는 "마감이 임박했습니다"(더한 어휘는 붙여 쓴 `마감 임박`).
어휘를 어디까지 넣을지가 그 두 건으로 고정된다.

## 셋이 결함을 재현하는지 동결 전에 확인했다

72건 초안을 라벨 없이 먼저 엔진에 던졌고, **여덟 건이 겨냥한 결함을 재현하지 않아
고쳤다.** 두 종류다.

- **행동 표가 절대 내보내지 않는 이름을 요구한 라벨** — v0.8 `fh-660` 과 같은 실수다.
  `money_transfer_request` 는 `DO_NOT_FORWARD_MONEY` 를 내지 않는다. 그 코드는
  `receive_and_forward_money` 신호나 `received_unknown_money` 상태에서만 나온다
  (`fh-810`·`fh-817`·`fh-830`·`fh-835`).
- **문장이 엔진 어휘를 빗나간 사례** — `fh-826`("보내 줄 수 있어?" 는 부탁형 의문문이라
  요구로 세지 않는다), `fh-828`(`빼서 … 넣어` 는 권한 위임에 쓰는 순서 게이트이지
  재전달 어휘가 아니다). `fh-828` 은 네 번 다시 썼다.

**결함을 재현하지 않는 사례는 수정의 값을 재지 못하고 만점만 하나 더 얹는다.**

## 얼린 뒤에 무결성을 수리했다

동결 커밋이 데이터셋과 baseline 은 넣었는데 **얼린 셋을 지키는 테스트를 등록하지
않았다.** 등록하자마자 두 가지가 걸렸다.

- `fh-839`·`fh-846` 이 v0.9 의 `fh-741`·`fh-767` 과 **본문이 같았다.** 하필 v0.9 가
  기록해 둔 오탐 둘, 즉 이번 회차가 갚으려던 바로 그 문장이다. **다시 쓰지 않고 뺐다** —
  수정이 존재하는 지금 쓰는 문장은 수정을 알고 쓰는 문장이고, 이 둘은 애초에 새로 재는
  것이 없었다. 그래서 두 ID 는 결번으로 남고 셋은 70건이다.
- persona 가 셋 중 둘뿐이었다. 여섯 건에 `unknown` 을 붙였다. 평가기는 persona 를
  엔진에 보내지 않고 통계로만 쓰므로(`evaluation/fraud_benchmark.py`) **측정된 숫자는
  한 칸도 움직이지 않고** 셋의 신원(해시)만 바뀐다.

> **얼리는 커밋에 얼린 것을 지키는 테스트가 없으면, 얼린 것이 아니라 적어 둔 것이다.**

수정 전 baseline 은 동결 커밋(`c6e5ede`)의 엔진을 체크아웃해 다시 쟀다. 재현 절차는
`docs/devlog/2026-08-24/fraud-holdout-v1.0.md` §7-3 에 명령까지 적혀 있다.

## 이 셋이 재되 이번 회차에 고치지 않은 것

- `fh-803` 미탐 — 고립 어휘가 "가족에게 말씀하시면 안 됩니다" 를 받지 않는다.
- `fh-806` 미탐 — `-하셔야 합니다` 종결형이 `DEMAND_MARKERS` 에 없다.
- `fh-825` — `보내 줘` + 계좌 목적지가 `money_transfer_request` 를 켜지 않는다.
  사기로는 잡히지만 신호 이름이 빈다.
- `money_transfer_request` 단독에 대응하는 유형이 없다. **스키마상 사례로 적을 수
  없어**(`is_fraud` 는 유형 유무와 일치해야 한다) 백로그에만 남긴다. `fh-837` 이
  원래 그 자리를 겨냥했는데 기관 자칭을 더해 돌렸다.

## 출처와 권리

FinShield 팀이 2026-08-24 직접 작성한 합성 문장이다. 실제 피해자 메시지, 개인정보,
외부 데이터셋 문장을 복사하지 않았다. 기관명은 실재하지만 문장은 전부 창작이며,
**실제 그 기관이 보낸 안내가 아니다.** 링크는 접속되지 않는 형태로 적었고, 서버는
이 문자열을 절대 열지 않는다. v0.1~v0.9 와 본문·ID 가 하나도 겹치지 않으며 그 사실은
테스트로 고정돼 있다 — 이번 회차에 그 테스트가 실제로 두 건을 잡아냈다.

## 이용 한계

합성이고, 70건이며, 한국어 문자·메신저 형식에 한정된다. 저자가 엔진 어휘를 알고 있다.
수정 후 이진 판정 f1 0.9730 은 **한 방향으로만 치를 값을 확인한 결과**이지 일반화된
성능이 아니다. 이 셋에서 읽을 칸은 `risk_ceiling_accuracy`(0.7813 → 1.0000)와
정상 32건의 오탐(7 → 0)이다.

CI 는 앞선 셋들과 마찬가지로 이 셋에 성능 기준선을 걸지 않는다.
