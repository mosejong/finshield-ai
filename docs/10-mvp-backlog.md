# 10. MVP Backlog

공개 배포까지 남은 항목의 순서·완료 기준은 `28-production-readiness.md`에 있다. 이 문서는 기능 범위, 28은 운영 준비 상태를 다룬다.

## P0 — 반드시 구현
- [x] FinancialProfile CRUD v0.1 (process-local prototype)
- [x] FinancialProfile SQLAlchemy·Alembic 영속화 + application-level 인증 암호화
- [x] 공식 금융상품 API adapter (live 검증 2026-08-12)
- [x] 최신월 공식 상품 live data profile (325건, 2026-08-12)
- [x] Product normalization + latest-month in-memory cache
- [x] source identity integrity / conservative duplicate policy
- [x] 사용자 goal 기반 conservative deterministic filtering v0.1
- [x] 원리금균등/원금균등 대출 시뮬레이터
- [x] Fraud text risk-signal extraction
- [x] Scenario Engine v0.1
- [x] 공식 근거 기반 설명
- [x] 분석 결과 provenance/source
- [x] pytest + CI
- [x] 익명 계정 전체 삭제 + 만료 세션/profile dry-run 정리
- [x] Docker·PostgreSQL runtime + migration + backup/restore CI
- [x] HTTP security headers + same-origin state-change protection + HTTPS deployment config
- [ ] public MVP deployment (real domain/DNS/certificate verification required) — 선행 P0는 전부 완료. 배포 절차·staging 예행연습·외부 검증기는 `31-public-deployment.md`에 준비돼 있고 `localhost` 예행연습까지 끝났다. 남은 것은 도메인·서버와, always-free `e2-micro`(1GB)를 고르면서 선행조건이 된 이미지 빌드·배포 파이프라인이다 (`28` P1-3)
- [x] rate limiting + 요청 본문 크기 제한 (인증 없는 `/api/v1/analyze`·세션 발급 보호)
- [x] 만료 데이터 정리 자동 스케줄 (`adr/0004` 이행)
- [x] 운영 backup 스케줄 + 복원 리허설 (합격 기준은 "복원됐다"가 아니라 "복호화됐다")
- [x] 파이썬 의존성 해시 잠금 + 런타임·개발 의존성 분리

## P1 — 경쟁력
- [x] Legacy rule vs Scenario Engine bootstrap benchmark (합성 61건, non-held-out)
- [x] persona·전체 UserState scenario golden set v0.1 (합성 bootstrap)
- [x] 고정 LLM-only vs Hybrid 비교 (2026-08-19, non-held-out 61건. `docs/32`)
- [x] 규칙 신호 어휘 v0.2 — 비교가 찾아 준 오답 3건을 규칙으로 메움 (2026-08-19)
- [x] **held-out v0.2 골든셋** (2026-08-20, 72건) — 동결 후 첫 측정에서 개발셋 1.000 이 기억이었음을 확인: precision 0.854 / recall 0.795 / 오탐률 0.214 / 상태 정책 정확도 0.681. `docs/32`
- [x] **held-out v0.3** (2026-08-20, 60건) — 엔진 수정 **전에** 동결하고 수정 전 baseline 을 같은 커밋에 넣었다. 커밋 순서가 증거다
- [x] **held-out 이 드러낸 엔진 결함 수정** (2026-08-20) — 네 가지 모두 닫았다. v0.3 에서 f1 0.677 → 0.984, 오탐 10건 → 0건. **v0.2 는 소진됐다.** 이 숫자는 표적 회귀 셋의 값이지 일반화 성능이 아니다. `docs/32`
- [x] **held-out v0.4** (2026-08-21, 60건) — 투자·지인 사칭 탐지가 **존재하기 전에** 동결하고 구현 전 baseline 을 같은 커밋에 넣었다. 커밋 순서가 증거다
- [x] **투자·리딩방 / 지인 사칭 유형** (2026-08-21) — `investment_scheme`·`acquaintance_impersonation` 을 taxonomy 에 넣고 신호 3종(`guaranteed_return_offer`·`private_channel_invite`·`familiar_person_claim`)과 액션 `VERIFY_BY_KNOWN_CONTACT` 로 구현. v0.4 에서 f1 0.655 → 0.946, 미탐 17건 → 1건, **오탐률 0.125 불변**. v0.2·v0.3·개발셋 수치는 한 자리도 안 바뀜. `docs/32`
- [x] **held-out v0.5** (2026-08-21, 72건) — 아래 네 결함을 고치기 **전에** 얼리고 수정 전 baseline 을 같은 커밋에 넣었다. 정상 28건(39%)을 네 수정이 깨질 자리에 몰아 놓아서, **수정 전 오탐률 0.3929 는 성능이 아니라 가격표다**. v0.4 소진
- [x] **활용형·어휘 폭 보강** (2026-08-21) — "폰 고장"·"번호 바꿨"·"배 확정"·"오픈채팅방 입장"·"깔아"·"비번" 등. 한국어 어미가 앞 음절에 합쳐지므로 기본형이 활용형의 접두사가 아니다("메워 드리" 는 "메워 드립니다" 를 못 잡는다)
- [x] **반말 요구 표지** (2026-08-21) — `CASUAL_DEMAND_PATTERN`. 존댓말 목록을 넓힌 게 아니라 **거울**로 만들었다("-아/어 주다" 반말 활용 + "-아/어 놓다" 명령형, 맨 명령형 어미 제외). 지인 사칭은 거의 전부 반말이라 게이트가 **가장 잡아야 할 사기에서 가장 자주 안 열렸다**
- [x] **예방 안내문 표지 보강** (2026-08-21) — 어간이 붙은 서술형만 넣었다. **맨 "하지 않" 은 넣지 않는다** — 넣으면 "소명하지 않으면 계좌가 동결됩니다" 가 예방 안내문이 된다. v0.5 양성 6건이 6개 유형에 걸쳐 이 실수를 즉시 드러내도록 배치돼 있다
- [x] **요구·대상을 절 단위로 묶기** (2026-08-21) — **거리로는 못 가른다.** `fh-321`(사기)과 `fh-356`(정상)은 요구·대상 거리가 같고, 진짜 사기는 요구를 마지막 절에 몰아 쓴다. 가르는 것은 **절의 시제**이며, 절 끝에 붙은 과거형만 센다. v0.5 f1 0.625 → 0.935, v0.4 미탐 1건 → 0건, v0.2 오탐 2건 → 0건. `docs/32`
- [x] **필수 signal 코드 이름 오류** (2026-08-21) — v0.4 11건·v0.5 24건이 응답에 실리지 않는 **내부 규칙 이름**을 요구하고 있어서 `required_signal_coverage` 가 탐지가 아니라 이름 불일치를 재고 있었다. 허용 목록을 규칙 표에서 끌어오고 검증기로 고정
- [x] **held-out v0.6** (2026-08-22, 72건) — 아래 세 결함을 고치기 **전에** 얼리고 수정 전 baseline 을 같은 커밋에 넣었다(`ef60a5e`). **이번 회차는 수정의 방향이 반대라 안전장치도 반대다** — 셋 중 둘이 좁히는 수정이고 그 값은 정상 문장이 아니라 사기 쪽에서 치러지므로, 부정 사례 수가 아니라 **좁히기가 끊을 수 있는 자리에 놓인 양성 사례 수**를 테스트로 고정했다. v0.5 소진
- [x] **`private_channel_invite` 의 투자·매매 맥락 조건** (2026-08-22) — 조사 붙은 활용형(`단톡방으로`·`오픈채팅방 입장`)을 목록에서 지우고 맨 방 이름을 새 게이트 `investment_gated_keywords` 로 내렸다. **매매 맥락 어휘에 "투자"와 "주식"은 넣지 않는다** — 정상 문장이 매일 쓰는 말이라 방 이름과 만나면 학부모 단톡방이 걸린다. v0.4 `fh-244` / v0.5 `fh-308` 이 남긴 질문이 여기서 닫혔다
- [x] **`authority_impersonation` 의 게이트 모양** (2026-08-22) — **자칭 신분은 요구의 대상이 아니다.** (a) 절 시제로 가릴 대상이 아니므로 메시지 **전체**에서 찾고, (b) 게이트를 요구의 *존재*가 아니라 요구의 *내용*(인증정보·계좌·앱 설치·원격·송금·의심스러운 링크)으로 좁혔다. 규칙 표에 `request_gated_keywords` 를 두고 탐지를 두 바퀴로 나눴다 — 한 바퀴면 규칙 표의 **순서**가 판정을 바꾼다. v0.6 유형 f1 0.526 → 0.947, v0.2 `fh-005` 신호 손실 닫힘
- [x] **공식 창구 안내 절을 요구에서 빼기** (2026-08-22) — **정상 기관은 이미 아는 창구로 보내고 사기는 자기가 만든 창구로 부른다.** 창구 이름 + 방향 조사 + 안내 서술어를 같은 절에서 볼 때만 적용한다(메시지 단위로 보면 안전한 첫 문장 하나로 나머지가 통과한다). 거울상인 "담당자 번호로 연락" 계열은 반대로 민감 요구로 친다. v0.5 `fh-360`·`fh-361` 오탐 닫힘
- [x] **`HIGH_RISK_SIGNAL_COMBINATIONS` 기존 조합 확장** (2026-08-22, `8257632`) — **기관은 자칭과 같은 메시지에서 넘겨 달라고 하지 않는다.** `authority_impersonation` 이 중간 하한을 세우고 민감 요구 신호도 중간 하한을 세우는데 `max` 는 중간 둘을 더하지 않아서, 소진된 다섯 셋에 걸쳐 15건이 하한에 미달해 있었다. **조합 목록은 사례를 세서 만들지 않았다** — `SENSITIVE_REQUEST_SIGNALS` 를 그대로 짝지었고, 그 집합을 parametrize 한 테스트가 두 표의 어긋남을 막는다. 탐지 무변경 정책 단독 커밋. 일곱 셋에서 **39건이 high 로 올라갔고 전부 사기**, 정상 문장은 0건, v0.7 천장 위반 0건
- [x] **재는 자의 천장** (2026-08-22, `0ce5188`) — 위 정책 확장은 등급을 **올리는** 수정인데 `_scenario_policy_accuracy` 가 하한에 `>=` 만 보므로 **모든 메시지에 `high` 를 찍는 엔진이 그 지표에서 1.0 을 받는다.** 넓히기와 좁히기는 값을 치를 자리가 있었는데 올리기만 없었다. `expected_max_risk` + `risk_ceiling_accuracy` 를 셋보다 먼저 넣었다. 천장은 **정상 문장에만** 걸고(사기를 높게 매기는 것은 신중함이다), 선언이 없는 셋에는 `1.0` 이 아니라 **`None`** 을 돌려주며(재지 않은 것을 만점으로 적으면 그것이 지어낸 근거다), 미선언 필드는 `normalized_dataset_sha256` 에서 뺀다(선언하지 않은 라벨은 라벨이 아니다 — 안 그러면 여섯 셋의 신원이 한꺼번에 바뀐다)
- [x] **held-out v0.7 동결** (2026-08-22, 72건, `0ce5188`) — 위 정책 확장과 아래 두 결함을 고치기 **전에** 얼리고 수정 전 baseline 을 같은 커밋에 넣었다. **정상 36건 전부에 등급 천장을 걸었고**, 그룹 A 10건을 전부 `received_only` 로 두어 상태 하한이 정책 수정을 대신 해 주지 못하게 했다. v0.6 소진
- [x] **`receive_and_forward_money` 어휘 구멍** (2026-08-22, `83298e8`) — **재전달을 만드는 것은 동사도 목적어도 아니고 요구다.** 어휘를 넓히면서 돈 게이트가 돈 언급만 보고 있던 것을 함께 고쳤다 — `자동이체` 가 `이체` 로 그 조건을 만족시켜서, "제 적금 계좌로 옮겨 두었어요" 같은 자기 돈 이동이 통과하고 있었다. 넓히기의 안전장치로 **요구**를 조건에 넣고, `옮기세요` 처럼 요구가 동사에 붙은 자리는 게이트가 이미 어휘를 좁혀 둔 안에서만 읽는다(맨 명령형 어미를 전역 목록에 넣으면 `확인하세요` 가 전부 요구가 된다). v0.3 `fh-138`·v0.6 `fh-446` 닫힘, v0.3 이진 f1 1.0000
- [x] **`secrecy_isolation` 에 대응하는 사기 유형** (2026-08-22, `af358c4`) — v0.6 `fh-454`. 신호는 켜지는데 유형이 비어 이진 판정이 정상으로 나가고 있었다. **유형만 붙이면 안 됐다** — 동결 baseline 이 이 신호가 대외비·NDA 같은 평범한 비밀 유지 문장 넷에서 이미 켜져 있음을 먼저 기록했고, 유형을 붙이는 순간 그 넷이 전부 사기 판정이 된다. **비밀 유지는 정상이고 확인 차단이 신호다** — 어휘를 보편적 금지와 채널 장악으로 좁히고 유형(`isolation_coercion`)을 같은 커밋에서 함께 넣었다
- [x] **held-out v0.8 동결** (2026-08-23, 72건, `9e35a05`) — 아래 결함들을 고치기 **전에** 얼리고 수정 전 baseline 을 같은 커밋에 넣었다. **이번 회차는 값을 치를 자리가 양쪽이다** — 넓히기 다섯에 좁히기 하나이고, 동결 시점 precision 1.0000 / FPR 0.0000 은 올라갈 자리가 없다. 그래서 정상 36건을 **각 넓히기가 깨질 수 있는 자리에 하나씩** 배치하고 세 개의 통합 테스트로 그 배치를 셌다. 얼리기 **전에** 라벨 두 개를 고쳤다(`fh-643` 상태 미충족, `fh-660` 이 API 가 절대 내보내지 않는 이름을 요구). v0.7 소진
- [x] **서로를 기다리는 두 게이트** (2026-08-23, `3bc57f6`) — v0.7 `fh-508`. 어휘 한 줄로 막지 않고 `SignalRule` 에 `demand_gated_sequences` 를 더했다. 낱말 하나에 담기지 않고 사이에 다른 말이 끼는 요구를 **순서**로 본다 — 붙여 적으면 어떤 실제 문장도 잡지 못한다(v0.7 이 `빼서 넣어` 로 이미 겪었다). 권한 위임 자체는 정상 제도이므로 **수령자 표현을 어휘에 함께** 넣었다("영업점에서 위임장을 제출하셔야 합니다" 에는 수령자가 없다). 열다섯 조합은 손으로 적지 않고 곱했다 — 손으로 적으면 하나가 조용히 빠지고 빠진 것을 알 방법이 없다. 미탐 14 → 9
- [x] **예방 안내문 표지가 문장 전체를 덮는 것** (2026-08-23, `8243915`) — v0.7 `fh-523`. 절 범위 문제가 아니라 **어휘가 보조용언 자리를 넘어간** 것이었다. 맨 `드리지 않` 이 `말씀드리지`·`건드리지` 를 함께 잡는다 — 보조용언 구성이 아니라 한 낱말인데도. 어간이 아니라 **앞자리**를 보는 정규식으로 옮겼다. 이 수정이 오탐 하나를 만들었고(`fh-632` "OTP를 대신 입력해 드리지 않습니다" → high), 원인은 **같은 목록을 `_demanding_clauses` 도 쓰고 있다**는 것이었다 — 표지를 빼자 그 예방 절이 요구를 공급했다. 두 번째 호출 자리에도 걸어 닫았다. **C' 여섯 건이 정확히 이걸 잡으려고 있었다.** 미탐 16 → 14
- [x] **재전달·기관 어휘 구멍 4건** (2026-08-23, `304f418`·`5946595`) — `정산금`·`돌려보내`·`나눠 보내`·목적지 계좌를 한 커밋에(같은 규칙의 같은 조건이라 따로 재면 서로의 값을 가린다), `지검` 은 **조건부** 자칭 어휘로 따로. 이 수정도 오탐 하나를 만들었다 — `fh-625`("회비는 나눠 보내지 마시고…")가 자금 재전달 요구가 됐다. 같은 문장이 그 행동을 **하지 말라고** 한다. **어휘를 좁혀서 피하지 않았다** — `나눠 보내 주` 처럼 어미를 붙이면 그 어미만 잡고 다음 회차에 다른 어미로 뚫린다. 금지형은 어휘가 아니라 **자리**의 문제라서 `PROHIBITION_ENDINGS` 로 자리에서 봤다. `-지 않` 은 일부러 뺐다 — "보내지 않으면 계좌가 정지됩니다" 는 협박이다. 미탐 9 → 3
- [x] **선입금 요구 어휘 구멍** (2026-08-23, `202c5c1`) — v0.7 `fh-562`. **높임 명령형이 어느 목록에도 없었다.** `이체하셔`·`입금하셔`·`송금하셔` 는 부탁 어미도 반말 어미도 아니다. 미탐 3 → 0, `advance_fee_demand` 유형 f1 0.333 → 1.000
- [x] **`risk_score` 와 `risk_level` 의 분리** (2026-08-23, `6034290`) — **등급을 점수에서 다시 계산하는 방향은 택하지 않았다.** 그러면 여섯 회차에 걸쳐 쌓은 canonical 판단과 상태 하한이 전부 legacy 가중치 표로 되돌아간다. 대신 **점수를 등급이 함의하는 띠까지 올린다**(`score_floor_for_level`) — 점수는 등급의 근거가 아니라 등급을 거스르지 않는 표시값이 된다. 바닥은 `determine_risk_level` 이 돌려준 **뒤에** 깔린다(순서가 뒤집히면 상태가 올린 등급이 자기 근거를 만든다). 등급 계산이 점수 띠에서 출발해 올리기만 하므로 원래 `점수 띠 <= 등급` 이었고, 바닥으로 반대 방향도 성립해 **둘이 정확히 같아진다**. 그 불변식은 코퍼스 위의 테스트다. `test_legacy_baseline_exact_scores` 는 이제 API 가 아니라 채점기를 직접 부른다 — 그 표가 못 박으려던 것은 가중치이지 응답의 숫자가 아니었다. 지표 무변경
- [x] **목적지 계좌 회귀 보수** (2026-08-23, `55a3a2d`) — **옛 셋 재측정이 잡은 회귀다.** 목적지 계좌를 돈 목적어의 대안으로 들이자 v0.5 `fh-317`("계좌가 묶였어. 일단 안전계좌로 옮겨 놔")이 자금 재전달 요구가 됐다. 받은 돈이 없는 안전계좌 이체 요구다. 이진 판정과 등급은 그대로여서 움직인 지표는 signal coverage 하나(1.0 → 0.9836)였지만 손해는 **이름**에 있다 — `money_transfer_request` 가 `money_mule` 이 되면 아무것도 받지 않은 사람에게 "받은 돈을 보내지 마세요" 가 나간다. 목적지를 들인 이유(돈 낱말이 완료 보고 절에 들어가 걸러짐)는 유효하므로 조건을 절이 아니라 **메시지 전체**에 걸었다. 넓힌 자리는 그대로 두고 목적지 하나로 돈 이야기를 **만들어 내는** 것만 막는다
- [x] **행동 지표에 천장** (2026-08-23, `7369501`) — `required_action_coverage` 는 `required ⊆ predicted` 만 세므로 **행동을 전부 붙이는 엔진이 1.0 을 받는다.** v0.7 이 등급에서 찾은 것과 같은 모양이고, 이번 회차가 행동을 갈아 끼우므로 여기서 닫았다. 사례에 `forbidden_action_codes` 를, 지표에 `forbidden_action_avoidance` 를 넣고 **그 뒤에** 셋을 얼렸다 — 결과를 보고 자를 고르면 그 자는 자가 아니다. 금지를 선언하지 않은 셋에서는 `1.0` 이 아니라 **`None`** 이 나간다. v0.1~v0.8 정규화 해시 무변경
- [x] **held-out v0.9 동결** (2026-08-23, 72건, `c47fa5f`) — 아래 v0.8 결함들을 고치기 **전에** 얼리고 수정 전 baseline 을 같은 커밋에 넣었다. **이진 판정이 만점이라 남은 변별력이 이름·행동·등급 쪽에만 있으므로** 이 셋은 하나의 질문을 겨냥했다 — *확인하라는 조언이 어디를 가리키는가.* 같은 요구를 **이름 있는 쪽과 없는 쪽에 하나씩**(A 10건 ↔ A' 10건) 놓고, 지인 사칭 4건을 v0.4 회귀 가드로 뒀다. 얼리기 전에 72건을 라벨 없이 엔진에 던져 **여덟 건이 겨냥한 결함을 재현하지 않는 것을 확인하고 다시 썼다** — 결함을 재현하지 않는 사례는 수정의 값을 재지 못하고 만점만 하나 더 얹는다. v0.8 소진
- [x] **확인 행동을 창구가 있는 쪽으로** (2026-08-23, `ee788d4`) — v0.8 `fh-603`·`fh-627`~`fh-629`·`fh-631`. **틀린 행동은 없는 행동보다 나쁘다** — 이름을 대지 않은 상대에게 "공식 대표번호로 확인하세요" 를 말하면 사용자는 없는 창구를 뒤지다가 **메시지에 적힌 번호로 건다.** 신호 표에는 자리표시자만 두고 `resolve_verification_actions` 가 검출된 신호에서 확인 수단을 정한다. **자칭으로 가른 첫 안은 틀렸다** — 아홉 셋에서 71건이 공식 창구를 잃고 품질 게이트까지 떨어졌다. 대출·정책자금·카드 발급·투자 권유는 자칭이 없어도 조회 창구를 가리킨다. `credential_request`·`account_access_request` 는 그전까지 확인 행동이 **아예 없었다.** 고립 요구를 어느 쪽에 넣을지는 판단이 아니라 기록이었다(v0.7 `fh-523` ↔ v0.8 `fh-627` 이 정반대 라벨이고 **뒤에 적힌 판단을 따랐다**)
- [x] **위협 + 접근수단 요구는 자칭 없이도 high** (2026-08-23, `3928f0b`) — v0.8 `fh-663`. 재촉은 점수만 올리고 등급 하한을 세우지 않아 **정작 거절하기 가장 어려운 조합**이 medium 에서 멈춰 있었다. 이 짝에는 자칭이 없어 값은 천장에서 치러지는데, **아홉 셋 어디에서도 천장이 깨지지 않았고** 올라간 것은 7건이다(v0.9 `fh-725`~`fh-728`, v0.8 `fh-663`, v0.7 `fh-503`, v0.2 `fh-025`)
- [x] **고발은 요구가 아니다** (2026-08-23, `1719966`) — v0.8 `fh-638`. "귀하 명의로 대포통장이 개설되었습니다" 는 통장을 **달라는** 말이 아니다. 요구 게이트는 메시지 안 아무 데나 요구가 있으면 열리므로 뒤에 붙은 "이체하셔야" 하나로 앞 절의 고발 문구가 요구 대상이 됐다. 게이트는 그대로 두고 **대상 탐색만** 고발 문구를 지운 본문에서 하게 했다 — 진짜 요구는 고발 문구 **밖에** 적혀 있다. 위 올리기와 묶여 있다(좁히기가 없으면 고발 문장이 없던 유형으로 high 까지 올라간다)
- [x] **v0.9 회차 열 셋 재측정** (2026-08-23, `845aad1`) — 필수 행동 coverage 0.7500 → 0.9625, 금지 행동 회피 0.6585 → 0.9268, 상태 정책 정확도 0.7222 → 0.9444. **이진 판정 네 칸은 한 칸도 움직이지 않았고 오탐·미탐도 열 셋 어디에서도 움직이지 않았다.** 값은 옛 셋의 라벨에서 치렀다 — v0.7 0.9583 → 0.8611, v0.6 0.9837 → 0.9675, v0.2 0.9758 → 0.9636 이고 잃은 열한 자리가 전부 같은 라벨(기관도 상품도 없는 메시지에 공식 창구를 요구)이다. 그 라벨의 **반대**를 v0.8 이 결함으로 적어 두었으므로 v0.8 은 0.9306 → 1.0000 으로 올라갔다. **옛 셋의 라벨은 고치지 않는다** — 동결한 셋의 라벨을 나중에 고치면 그 셋은 더 이상 held-out 이 아니다
- [x] **held-out v1.0 동결** (2026-08-24, 70건, `c6e5ede`) — 아래 v0.9 결함들을 고치기 **전에** 얼리고 수정 전 baseline 을 같은 커밋에 넣었다. **새로 상상한 사기 어형이 하나도 없는 첫 셋이다** — v0.9 가 "다음 회차 몫" 으로 이름만 적어 둔 결함 다섯이 그대로 다섯 그룹이 됐다. 다섯 수정이 전부 넓히기라 값이 **한 방향으로만** 치러지므로 정상 32건 전부에 등급 천장을 걸었고 **그중 스물여섯 건이 넓히려는 바로 그 어형을 쓰는 정상 문장**이다. 얼리기 전에 72건을 라벨 없이 엔진에 던져 여덟 건을 고쳤다(넷은 행동 표가 내보내지 않는 이름을 요구한 라벨 — v0.8 `fh-660` 과 같은 실수). v0.9 소진
- [x] **금지 앞자락을 잘라 내고 남은 말로 판단한다** (2026-08-24, `7604f65`) — v0.9 `fh-741`·`fh-767`. 동결 시점 오탐 일곱 중 **다섯이 한 어형**(`-(으)면 안 됩니다`)이었다. 예방 안내문은 위험한 행동을 말리려고 그 행동을 입에 올려야 해서, **하지 말라는 문장이 요구를 세는 자리에서 요구로 세어졌다.** 어형을 하나씩 빼는 대신 `_strip_prohibited_segment` 로 금지까지 잘라 내고 `_has_reader_demand`·`_is_channel_referral` 이 나머지를 보게 했다 — 어형은 무한하고 어휘는 유한하다. 절을 통째로 버리지 않은 이유는 사기가 **금지 뒤에서** 요구하기 때문이고(`fh-802`), 같은 통찰이 **금지된 창구는 창구 안내가 아니다**로 반대편도 닫는다. 오탐 다섯과 미탐 하나가 한 수정으로 닫혔다
- [x] **긴급 어휘를 여섯에서 열둘로** (2026-08-24, `7604f65`) — v0.9 `fh-729`. `1시간 안에`·`30분 내`·`오늘 자정까지`·`내일까지`·`금일 중`·`마감 임박`. **`LEGACY_RULES`(점수를 만드는 표)는 일부러 건드리지 않았다** — 같은 시간 표현을 평범한 청구서도 쓴다(`fh-854`). 어휘는 등급 조합과 설명에만 쓰이고 정상 안내문의 점수는 0 그대로다
- [x] **띄어 쓴 `대환 대출` 은 요구 게이트에** (2026-08-24, `7604f65`) — 무조건 층에 넣지 않았다. 띄어 쓴 형태는 정상 안내문도 쓰는 형태다(`fh-860` "대환 대출 상담은 영업점 창구에서"). `지검` 을 두 층으로 나눈 것과 같은 이유다. 같은 커밋에서 `{loan_policy_offer, credential_request}` 조합도 채웠다 — **한도 조회에 비밀번호가 필요한 대출 창구는 없다**(`fh-821`)
- [x] **조건부 기관 자칭이 고립 요구만으로도 켜진다** (2026-08-24, `7604f65`) — v0.9 `fh-712` 에서 발견하고 사례로 만들지 않았던 것을 v1.0 이 `fh-822`~`fh-824` 로 처음 쟀다. 등급 조합에는 이미 `{authority_impersonation, secrecy_isolation}` 이라고 적혀 있는데 `SENSITIVE_REQUEST_SIGNALS` 에만 빠져 있었다. **넘겨줄 물건이 없을 뿐 요구가 없는 것이 아니다.** 기밀 유지는 여전히 넣지 않았다 — "외부에 공유하지 말아 주세요"(사내 공지)는 화제를 집단 안에 두는 말이지 제3자 확인을 막는 말이 아니다
- [x] **v1.0 회차 열 셋 재측정** (2026-08-24, `7604f65`) — v1.0 에서 오탐 7 → **0**, 미탐 3 → 2, f1 0.8750 → **0.9730**, 등급 천장 0.7813 → **1.0000**. **오탐이 늘어난 셋은 하나도 없다.** v0.9 는 **여섯 회차 묵은 빚**이 닫혀 이진 판정·등급 천장 만점이 됐고(오탐 2 → 0), v0.7 은 양방향으로 움직였다 — `fh-520`·`fh-522` 가 라벨이 처음부터 요구한 `VERIFY_OFFICIAL_CHANNEL` 을 내면서 상태 정책이 0.8611 → 0.8889 로 올랐고, 같은 두 건이 `authority_impersonation` 을 유형으로도 내면서 그 유형 precision 이 1.0000 → 0.8462 로 내려갔다. **잃은 쪽도 그대로 적고 옛 라벨은 고치지 않는다**
- [x] **얼린 셋을 지키는 테스트를 등록하고 무결성을 수리했다** (2026-08-24, `c77b782`) — 동결 커밋이 데이터셋과 baseline 은 넣었는데 통합 테스트에 셋을 등록하지 않았다. 등록하자마자 `fh-839`·`fh-846` 이 v0.9 의 `fh-741`·`fh-767` 과 본문이 같은 것이 걸려 **다시 쓰지 않고 뺐다**(수정이 존재하는 지금 쓰는 문장은 수정을 알고 쓰는 문장이고, 빼기는 결과를 좋게 만들 수 없는 유일한 편집이다). persona 가 셋 중 둘뿐인 것도 함께 걸려 여섯 건에 `unknown` 을 붙였다. **얼리는 커밋에 얼린 것을 지키는 테스트가 없으면, 얼린 것이 아니라 적어 둔 것이다**
- [x] **held-out v1.1 동결** (2026-08-24, 79건, `bc5900e`) — 아래 결함들을 고치기 **전에** 얼리고 수정 전 baseline 을 같은 커밋에 넣었다. **이번 회차의 발견은 엔진이 아니라 셋에 있었다** — 열 회차 동안 `received_only` 가 아닌 사례가 27건에서 6건까지 줄었고 **아무도 그것을 정하지 않았다.** 매 회차의 결함이 문장 안에 있었고 문장 안의 결함은 상태를 고정해야 깨끗하게 재지므로 회차마다 옳은 선택이었는데, 열 번 쌓이자 상태 정책 일곱 갈래 중 하나가 표본의 91% 를 차지하게 됐다. **지표가 만점에 가까울 때 물어야 할 것은 "무엇이 남았나" 가 아니라 "이 숫자의 분모가 무엇인가" 다.** v1.1 은 37건이 상태를 갖고 여섯 갈래가 각각 최소 4건이며 그중 열 건이 정상이다. 그러자 `scenario_policy_accuracy` 가 v1.0 의 0.9571 에서 **0.6582** 로 떨어졌다 — 엔진이 나빠져서가 아니라 재지 않던 것을 재기 시작해서다. v1.0 소진
- [x] **상태가 있는 정상 문장의 천장은 그 상태가 강제하는 바닥이다** (2026-08-24, `bc5900e`) — v0.7 이 천장을 도입했을 때 정상이 전부 `received_only` 라 천장도 전부 `low` 였다. 상태가 있는 정상에 `low` 를 걸면 그 라벨은 **상태 정책 자체를 결함으로 선언**한다. 계좌 정보를 이미 넘긴 사람의 등급이 `high` 로 강제되는 것은 맞는 일이고, **그 문장이 사기가 아니라는 사실은 등급이 아니라 유형과 행동으로 표현되어야 한다**
- [x] **상태가 예방 행동을 반복한다** (2026-08-24, `902837a`) — 일곱 상태 중 둘만 예방 행동을 냈다. 계좌 권한·앱 설치·개인정보·송금에는 그 자리가 비어 있었다. **이미 한 번 넘긴 사람이 가장 다시 넘기기 쉬운 사람이다** — 사기는 한 번으로 끝나지 않고 같은 통화 안에서 "이번엔 두 번째 계좌로" 가 이어진다. 단, **상태는 신호를 대신하지 않는다** — 위험 신호가 하나라도 켜진 메시지에서만 낸다. 계좌를 알려 주는 일도 앱을 까는 일도 송금하는 일도 대부분은 정상이다. 이 네 상태의 v1.1 정상 5건은 전부 신호가 비어 게이트가 닫히고, 게이트가 닫히는 사기 8건은 전부 **어휘 공백 목록에 이미 이름이 적힌** 건이다
- [x] **사건 어휘가 창구를 가리킨다** (2026-08-24, `902837a`) — 창구를 **기관 자칭 신호에서만** 찾고 있었는데 사기 문자의 절반은 자기가 누구인지 말하지 않고 **무슨 일이 일어났는지만** 말한다. 기관명이 없어도 갈 곳은 명확하다(카드 뒷면 번호·홈택스·112). v0.9 규칙이 그 자리에서 창구가 없다고 판단해 폴백으로 아는 연락처를 내므로 **미탐 하나가 틀린 행동 하나를 함께 낸다.** `확인`·`절차`·`신청` 은 **일부러 뺐다** — 너무 흔하고 무엇보다 창구를 지목하지 않는다. 등급은 올리지 않고, 신호가 이미 켜진 메시지에서만 읽는다
- [x] **고립을 어휘가 아니라 어형으로 본다** (2026-08-24, `902837a`) — v1.0 `fh-803`. 어휘 목록이 **동사에 바로 붙는 금지형**만 담아서 `가족에게도 알리지 마세요` 는 잡고 `가족에게 말씀하시면 안 됩니다` 는 통째로 놓쳤다. 고립의 **대상**과 **금지**를 따로 세우고 둘이 한 절 안에 있을 때만 켠다 — v1.0 이 요구를 세는 쪽에서 이미 만난 그 어형이고, **같은 어형이 두 회차에 걸쳐 두 자리에서 결함을 냈다.** `외부에` 는 v1.0 `fh-865`(사내 공지)와 부딪혀 넣지 않았고, `보내`·`전달` 도 뺐다 — 그것을 막는 문장은 고립이 아니라 보안 안내다
- [x] **금지의 대상이 읽는 사람이 쥔 비밀이면 고립이 아니라 보안 안내다** (2026-08-24, `902837a`) — 고립 어형을 넓히자마자 v0.9 `fh-741`("인증번호는 저희를 포함해 누구에게도 알려 주시면 안 됩니다", 한국에서 가장 흔한 정상 보안 문자)이 오탐이 됐다. 어형이 아니라 **글자까지** 겹친다. 첫 안이던 **화자를 예외로 두는가**(`저희를 포함해`)는 버렸다 — 사기 문자가 아무 비용 없이 흉내 낼 수 있다. 가르는 자리는 **금지의 대상**이다: 고립은 이 대화 자체를 말하지 말라고 하고 보안 안내는 읽는 사람이 이미 쥔 비밀을 말하지 말라고 한다. **인증번호를 말하지 말라는 문장은 인증번호를 요구하지 않는다.** 예외는 어형 층에만, 절 안에서만 적용한다(전체에서 보면 사기가 뒤에 "인증번호" 한 단어를 붙여 탐지를 끌 수 있다)
- [x] **`DEMAND_MARKERS` 의 종결형 계열은 결함이 아니었다** (2026-08-24) — v1.0 `fh-806` 이 남긴 예고를 v1.1 `fh-911`·`fh-912`·`fh-914` 로 쟀더니 **동결 시점에 이미 전부 통과한다.** `주셔야` 가 목록에 있었고 `-하셔야` 계열은 위험 행동 어간(`설치`·`이체`·`입력`)이 받고 있었다. 같은 층으로 묶어 뒀던 `fh-913` 하나만 실패하는데 원인은 종결형이 아니라 `공인인증서` 라는 어휘 공백이다. **얼린 셋은 예측을 반증하기도 하고, 없는 결함을 고치지 않는 것도 회차의 결과다**
- [x] **채널 어휘의 미등록 이름은 방향이 반대였다** (2026-08-24) — 구글플레이·원스토어를 `CHANNEL_REFERRAL_PATTERN` 에 넣으면 그 어휘가 등장하는 문장에서 **요구를 빼는** 쪽으로만 작용한다. 겨냥한 것과 반대다. 이 항목이 실제로 담고 있던 내용(정부24 가 창구 이름이다)은 위 「사건 어휘가 창구를 가리킨다」로 흡수했다
- [x] **v1.1 회차 열두 셋 재측정** (2026-08-24, `902837a`) — v1.1 에서 상태 정책 0.6582 → **0.8354**, 필수 action coverage 0.7869 → **0.8852**, 금지 행동 회피 0.9664 → **1.0000**, 미탐 19 → 15, f1 0.6875 → **0.7647**. **오탐이 늘어난 셋은 하나도 없다.** v1.0 이 "다음 회차 몫" 으로 이름을 적어 둔 `fh-803` 이 닫혀 그 셋의 f1 0.9730 → **0.9867** · `isolation_coercion` f1 0.8571 → **1.0000** 이 됐다 — **결함을 이름으로 적어 두면 다음 회차가 갚는다**가 두 번 연속 확인됐다. v0.2·v0.6·v0.7 의 상태 정책과 action coverage 도 함께 올랐다. **놓친 필수 행동 39건이 21건으로 줄었고 남은 21건은 하나도 빠짐없이 신호 누락의 하류다** — 행동 표 자체에 뚫린 구멍은 닫혔고 남은 것은 전부 어휘다
- [x] **얼린 라벨끼리 부딪힌 자리를 고치지 않고 고정했다** (2026-08-24, `902837a`) — v1.0 `fh-803` 과 v1.1 `fh-901` 이 사실상 같은 문장(기관 자칭 없음 · 수사 언급 · 고립 요구)에 `VERIFY_OFFICIAL_CHANNEL` 금지 / 요구로 **반대 라벨**을 달았다. v0.7 `fh-523` vs v0.8 `fh-627` 에서 세운 **"뒤에 적힌 판단을 따른다"** 로 v1.1 을 따랐다 — 수사 사칭 문자를 받은 사람에게 "이전부터 쓰던 연락처로 확인하세요" 는 실행할 수 없는 조언이다(수사기관과 이전부터 쓰던 연락처를 가진 사람은 없다). 그 값으로 v1.0 의 금지 행동 회피가 1.0000 → 0.9231 이 됐고 **숫자에 그대로 남겼다.** `test_v1_0_fh_803_is_a_recorded_label_disagreement_not_a_silent_regression` 이 이 한 건이 **기록된 불일치**라는 것과 **다른 불일치가 없다**는 것을 함께 못 박는다 — **얼린 셋을 태우지 않으면서 회귀와 불일치를 구분하는 방법은 불일치의 목록을 고정하는 것뿐이다**
- [x] **held-out v1.2 동결** (2026-08-24, 80건, `6635129`) — 아래 결함들을 고치기 **전에** 얼리고 수정 전 baseline 을 같은 커밋에 넣었다. **진짜 기관도 문자를 보낸다** — 열한 회차 동안 이 셋들은 사칭 문장만 모아 왔고, 그래서 `authority_impersonation` 의 무조건 층이 한 번도 값을 치르지 않았다. 경찰청·검찰청·법원·금감원이 실제로 보내는 통지문 열두 건과 **어휘가 거의 같은** 사칭 열 건을 짝으로 넣어 갈리는 자리를 하나만 남겼다. 동결 시점 accuracy **0.4875** · f1 **0.4384** · 오탐률 **0.3030** — 동전보다 조금 나은 수준이다. 다섯 예측이 전부 적중했고 **미탐 31건이 한 건도 빠짐없이 신호 0개**였다(잘못 매겨진 신호는 하나도 없었다). v1.1 소진
- [x] **무조건 층에 천장을 세운다 — 갈리는 것은 기관명이 아니라 창구다** (2026-08-24, `284aeba`) — v1.1 `fh-942` 가 이름을 적어 둔 자리이고, 열두 건을 같은 자리에 놓자 **잡음이 아니라 층 전체의 구멍**이었다(12건 중 8건 오탐). **진짜 기관은 읽는 사람이 이 문자 없이도 갈 수 있는 창구를 댄다** — 창구를 대는 것은 읽는 사람을 메시지 밖으로 내보내는 행위이고, 사칭은 대는 순간 거짓이 드러나므로 그러지 못한다. 자칭 기관이 갈 수 있는 창구를 대고 아무것도 요구하지 않으면 통지문이다. 함께 넓힌 것은 창구의 **이름**이다(이파인·형사사법포털·경찰민원포털·홈택스·민원실·파인) — 기존 목록은 종류 어휘뿐이라 **진짜 기관이 실제로 쓰는 말을 한 번도 못 잡았다**. 이 한 건을 고치는 데 80건짜리 셋 하나가 필요했다
- [x] **창구를 대는 것과 창구를 닫는 것은 반대 방향이다** (2026-08-24, `284aeba`) — **창구 이름은 억제의 표지인데, 그 표지를 그대로 갖추고 부정만 덧붙이는 어형이 실제로 있다**("홈택스 말고 저희 전용 시스템으로만", "1332는 지금 연결이 안 되니"). 억제 규칙을 노리는 가장 싼 우회다. 같은 검사를 `_is_prevention_notice` 에도 걸었다 — `fh-1052` 는 `공식 홈페이지` 라는 낱말 하나로 모든 신호를 껐는데 문장이 하는 말은 정확히 그 반대였다
- [x] **창구를 화자 하나로 좁히는 것은 고립 요구다** (2026-08-24, `284aeba`) — 고립 어휘는 `저와만 연락` 같은 어형 몇 개뿐이었는데 실제 어형은 훨씬 흔하다: **화자를 가리키는 말에 배타 조사가 붙는다**("이 번호로만", "담당자를 통해서만", "저희 담당 창구에서만"). 어휘가 아니라 자리로 적었다. **진짜 기관은 자기를 유일한 통로로 만들지 않는다** — 그럴 필요가 없다. 같은 어형이 **창구 안내 절 판정에서도 반대로 작동**해서, 그 절이 안내로 세어지는 동안 그 안의 고립 어형은 아예 보이지도 않았다(`fh-1020`·`fh-1050`). **억제와 탐지가 같은 표지를 공유하면 억제가 먼저 도는 한 탐지는 그 자리를 영원히 못 본다**
- [x] **`외부에` 충돌을 금지의 대상으로 결정했다** (2026-08-24, `284aeba`) — v1.0 `fh-865` 와 v1.1 `fh-906` 이 넘긴 숙제다. **고립이 막는 것은 이 연락 자체이고**(이 건·이 통화·저희가 안내드린 절차), **기밀 유지가 막는 것은 읽는 사람이 이미 쥔 것이다**(초안·합격 사실·협의 내용·감사 내용). 빗장을 셋 건다 — 대상, 어미, 남겨 두는 집단. **금지여야 한다, 보장은 금지가 아니다**(v0.7 `fh-535` "상담 내용은 외부에 공개되지 않습니다" 가 첫 안에서 오탐이 됐고 어미 잠금으로 되돌렸다). **남겨 두는 집단이 있으면 고립이 아니다**(`fh-1028` "팀원들 외에는 아무에게도" — 전칭 금지 어휘를 갖췄지만 읽는 사람 곁에 남는 사람이 있다)
- [x] **상태만으로 상대를 끊으라고 하지 않는다** (2026-08-24, `284aeba`) — 예측에 없던 결함이다. 신호가 하나도 없는 정상 문장이 상태만으로 `STOP_CONTACT` 를 받아 **회사와·병원과·거래처와 연락을 끊으라는 말**이 나갔다(`fh-1059` 급여 입금, `fh-1064` 진료 예약, `fh-1065` 거래처 대금). 상태 표의 나머지 행동은 전부 읽는 사람 쪽에서 끝나는데 **`STOP_CONTACT` 만 방향이 반대라 상대가 적이라는 판단이 먼저 있어야 하고, 상태는 읽는 사람이 무엇을 했는지만 말한다.** v1.1 이 `STATE_PREVENTION_ACTIONS` 에서 세운 **상태는 신호를 대신하지 않는다**를 한 칸 더 민 것이고, 등급 바닥(`STATE_MINIMUM_RISK`)은 건드리지 않았다 — **재는 것은 등급이 아니라 행동이다**
- [x] **v1.2 회차 열두 셋 재측정** (2026-08-24, `284aeba`) — v1.2 에서 f1 0.4384 → **0.7532**, 오탐 10 → **1**, 오탐률 0.3030 → **0.0303**, 미탐 31 → 18, signal coverage 0.3860 → **0.6842**, 등급 천장과 금지 행동 회피 **1.0000**. v1.1 도 함께 올라 f1 0.7647 → **0.8286** · 오탐 1 → **0** 이 됐다 — **결함을 이름으로 적어 두면 다음 회차가 갚는다**가 세 번 연속 확인됐다. **유형별로 움직인 것은 둘뿐이고**(`isolation_coercion` 0.2222 → 0.9677, `authority_impersonation` 0.3077 → 0.7273) 나머지 여덟 유형이 한 자리도 안 움직였다 — 그 여덟이 정확히 어휘 공백이라 **귀속이 깨끗하다**
- [x] **상태로 부딪힌 얼린 라벨을 고치지 않고 남겼다** (2026-08-24, `284aeba`) — v0.3·v0.4 의 **정상** 15건이 상태만으로 `STOP_CONTACT` 를 요구해서 상태 정책 정확도가 0.9333 → 0.8167 · 0.8667 → 0.7333 으로 내려갔다. 13건은 그 셋의 기준으로 봐도 틀렸다(집주인과·인사팀과·증권사와 연락을 끊으라는 말이었다). **나머지 둘(`fh-211`·`fh-228`)은 틀리지 않았고 이번 수정으로 잃는다** — 피해자의 1인칭 서술이라 어떤 신호도 켜지지 않는다. **얼린 셋은 어느 방향으로도 맞춰 쓰지 않는다** — 라벨을 고치지 않고 떨어진 점수를 결과 파일에 그대로 남겼다. **"뒤에 적힌 판단을 따른다"** 로 v1.2 를 따랐고, **두 방향을 다 적어 두는 것이 그 규칙의 값이다**
- [x] **held-out v1.3 동결** (2026-08-25, 84건, `a0e0f22`) — 아래 결함들을 고치기 **전에** 얼리고 수정 전 baseline 을 같은 커밋에 넣었다. v1.2 가 남긴 숙제를 셋의 **설계 원칙**으로 올렸다 — **이번 회차가 넓히려는 자리마다 그 넓히기가 부술 수 있는 정상 문장을 먼저 넣는다.** 여덟 묶음 전부가 사기와 정상의 짝이고, 짝이 없는 수정은 이 회차에 하지 않았다. 동결 시점 accuracy **0.7024** · f1 **0.6032** · precision **0.9500** · recall **0.4419** — v1.2 와 같은 모양으로 **잘못 매긴 신호가 아니라 없는 신호**가 문제다(미탐 24건 대부분이 신호 0개). A군 10건 전부 조용했고 J군 회귀 가드 21건이 전부 버텼다. v1.2 소진
- [x] **요구가 없어도 읽는 사람이 시작하지 않았으면 사칭이다** (2026-08-25, `7a06137`) — v1.2 는 기관 자칭을 요구가 있을 때만 켜기로 했고 진짜 기관은 갈 수 있는 창구를 댄다는 것으로 천장을 세웠다. **두 조건이 통째로 비켜 가는 자리가 있다** — 아직 아무것도 요구하지 않고 연락하겠다고만 하는 문자다. 요구는 다음 통화에 온다. A군(정상 10)과 B군(사기 10)은 기관 이름·문체·요구 없음이 같고 **창구를 대지 않는 것까지 같다**(A군 열 건 전부 `_names_reachable_counter` 가 거짓이라 창구 조건이 이 자리에서 아무 일도 하지 않는다). 남는 표지는 **누가 이 일을 시작했는가** 하나다 — 진짜 기관은 신청·문의·접수를 받은 뒤에 연락하고 사칭은 아무것도 받지 않은 사람에게 먼저 건다. `드리` 로 적었다가 B군 넷을 놓쳤다(드리+ㅂ니다 → **드립**니다) — v0.4 `메워 드리`·v0.5 `폰이 고장` 과 같은 함정이고 **세 번째다**
- [x] **받은 돈이 없으면 재전달이 아니다** (2026-08-25, `7a06137`) — `옮겨`·`넘겨` 는 재전달의 동사이기도 하고 제 돈을 움직이는 동사이기도 해서 금액과 요구만으로는 갈리지 않는다("들어온 돈은 그대로 다른 계좌로 넘겨 주시면 됩니다" 대 "잔액을 국가안전계좌로 옮겨 주십시오"). 아홉 번째 게이트 `receipt_gated_keywords` 와 `RECEIPT_MARKERS` 로 갈랐다 — **얼려 둔 열세 건이 이 한 줄로 갈리고**(`money_mule` 일곱 건은 예외 없이 수취 표지가 있고 `money_transfer_request` 여섯 건은 하나도 없다) 조건을 세우기 전에는 `fh-317`·`fh-937`·`fh-1048` 이 라벨과 다른 이름으로 새어 나가고 있었다. **판정만 맞고 행동이 틀리면 이 제품이 하는 일이 남지 않는다** — 앞은 "받은 돈을 보내지 마세요"이고 뒤는 "안전계좌라는 것은 존재하지 않습니다"이다. v0.8 은 목적지 조건을 좁혀 이 문제를 피했고 v1.3 은 정면으로 갈랐다
- [x] **호칭과 자기지목은 요구가 붙어야 신호다** (2026-08-25, `7a06137`) — 여덟 번째 게이트 `demand_gated_patterns`. 기존 `patterns` 는 무조건 층이라 **정상 문장과 어형까지 같은** 이 자리에 쓸 수 없다. 진짜 형은 "형 나야, 내일 몇 시에 볼까"라고 쓰고 사칭은 "형 나야, 200만원만 보내 줄 수 있어"라고 쓴다 — 사기 4건 전부 요구가 있고 정상 4건 전부 없다. 금액 조건은 쓸 수 없었다(`_mentions_money` 가 `200만원` 을 세지 않았고 `fh-1128` 은 돈 이야기 자체가 없다) — 그 구멍은 `MONEY_AMOUNT_PATTERN` 으로 따로 닫았다
- [x] **조건이 먼저 서야 어휘를 넓힐 수 있다** (2026-08-25, `7a06137`) — `반환` 을 금액 조건에 걸었다가 둘을 잃었다: v0.7 `fh-550`("잘못 입금하신 금액은 은행을 통해 반환 절차가 진행됩니다")은 **재전달을 말리는** 문장인데 오탐이 됐고, v0.2 `fh-017`("전액 반환됩니다")은 돌려주겠다고 약속하는 쪽이 보내는 사람인데 신호 이름이 바뀌었다. **반환은 받은 것이 있어야 성립하는 말**이라 수취 조건이 둘을 다 막았다 — 낱말을 좁히는 대신 이 회차가 이미 세운 문장을 한 번 더 썼고, 조건이 서자 v1.2 착오송금 세 건이 덤으로 닫혔다. 그 뒤 `잘못 입금` 을 수취 표지에 넣었다가 `fh-550` 이 다시 오탐이 됐다 — **값을 치를 사례가 없는데 넣은 것**이라 빼고 그 자리에 다시 넣지 말라는 주석을 남겼다
- [x] **낱말이 아니라 자리로 적는다** (2026-08-25, `7a06137`) — 인증정보 어휘를 넓히면서 `카드 뒷면`·`신분증` 을 그대로 넣었더니 얼려 둔 정상 셋이 오탐이 됐다(v0.9 `fh-742`·v1.0 `fh-870` "카드 뒷면 번호로 연락 주세요", v1.0 `fh-850` "신분증을 지참해 주세요"). **카드 뒷면에는 CVC 도 있지만 고객센터 번호도 인쇄돼 있고, 신분증은 넘기는 물건이기 전에 창구에 들고 가는 물건이다.** `뒷면 세 자리`·`신분증 사진` 으로 좁혔고 덤으로 v1.1 `fh-909` 가 닫혔다. **이 회차의 `fh-1160` 이 그 자리를 미리 적어 두고 있었다** — 셋을 만들 때 알고 있었는데 어휘를 넣을 때 잊었다
- [x] **v1.3 회차 열두 셋 재측정** (2026-08-25, `7a06137`) — v1.3 에서 f1 0.6032 → **1.0000**, 오탐 1 → **0**, 미탐 24 → **0**, signal coverage 0.3704 → **1.0000**, 시나리오 정책·등급 천장·금지 행동 회피 전부 **1.0000**, p95 는 3.425 → 3.484ms 로 움직이지 않았다. **어느 셋도 동결 시점보다 나빠지지 않았고 이번 수정이 만든 오탐은 열두 셋 통틀어 0 이다** — 남은 오탐 5건(v0.4×1·v0.5×3·v0.6×1)은 전부 이번 회차 이전부터 있던 것이다. v1.1 f1 0.8286 → **0.9211**(미탐 12 → 6), v1.2 0.7532 → **0.9783**(미탐 18 → 2) 이 이번에 세운 조건이 **다른 셋에서도 선다**는 증거다 — **결함을 이름으로 적어 두면 다음 회차가 갚는다**가 네 번 연속 확인됐다. **1.0000 을 좋은 소식으로만 읽지 않는다** — 이 셋이 물은 것을 다 맞혔다는 뜻이지 물어야 할 것을 다 물었다는 뜻이 아니다
- [x] **개발셋 라벨 하나를 옮겼다** (2026-08-25, `7a06137`) — `fg-004`("조사 협조를 위해 자금을 지정 장소로 옮기세요")는 `money_mule` 로 적혀 있었는데 **받은 돈이 없으므로** `money_transfer_request` 가 맞다. v0.8 의 주석이 이미 그렇게 적고 있었는데 개발셋 라벨이 따라가지 않았다. **얼려 둔 셋의 라벨은 어느 방향으로도 고치지 않는다** — 이것은 개발셋이고 이 회차가 세운 구분이 드러낸 오분류라 옮겼으며, 옮긴 자리에 그 이유를 적은 guard 테스트를 남겼다
- [ ] **`문의하신` 을 거짓으로 붙이는 사칭을 재는 짝을 다음 셋에 넣는다** — 조건부 자칭 게이트가 기대는 표지는 **사칭이 흉내 낼 수 있는 표지**이고, v1.3 에는 그것을 흉내 낸 사기가 한 건도 없어 값을 재지 못했다. 게이트는 세우되 한계를 주석에 적어 두었다. **카드 배송 쪽에서는 이미 흉내 내고 있다**(아래 항목) — 기관 자칭 쪽에서도 흉내 낼 것이라고 보는 편이 안전하다. **v1.4 의 첫 요건**
- [ ] **1.0000 을 깨는 셋을 만든다** — v1.3 은 84건 전부를 맞혔다. 묶음마다 값을 놓는 방식은 유지하되 **동결 시점 예측이 다 맞지 않는 셋**을 만들어야 한다. **재는 셋 안의 결함을 다 고치면 그 셋은 성능이 아니라 기억을 재게 된다**
- [x] **창구 부재를 잴 정상 문장을 다음 셋에 넣는다** (2026-08-25, `a0e0f22`) — v1.2 `fh-1013`~`fh-1016`(국세청·건강보험공단·관세청·우체국 자칭 + 창구 없음 + "담당자가 연락드리겠습니다"). **어형은 분명하고 규칙도 썼다가 커밋하지 않았다** — F군 15건 중 기관을 자칭하면서 창구 없이 회신을 약속하는 **정상** 문장이 하나도 없어 값을 잴 수 없었다. 실제 세계에는 있다("접수되었습니다. 담당자가 확인 후 연락드리겠습니다" 는 정상 고객센터의 흔한 문장이다). **측정할 수 없는 수정은 고친 것이 아니라 옮긴 것이다.** v1.3 이 A군 10건으로 그 자리를 만들었고, 값이 서자 이 네 건과 D4 14건이 함께 닫혔다
- [x] **`card_delivery_claim` 무조건 층에도 천장을 세운다** (2026-08-25, `7a06137`) — v1.2 `fh-1058`("신청하신 카드가 발급되어 등기로 발송되었습니다. 배송 조회는 카드사 앱에서 가능합니다.")가 유일하게 남은 오탐이다. 요구 게이트가 없는 무조건 층인데 D1 이 `authority_impersonation` 에 세운 천장이 여기엔 없다. 창구 조건으로 닫았다 — 다만 **표지는 이 항목이 떠올렸을 `신청하신` 이 아니었다.** 그 표지를 여기에 걸었더니 얼려 둔 사기 일곱 건이 한꺼번에 미탐이 됐다(`fh-015`·`fh-042`·`fh-232`·`fh-420`·`fh-452`·`fh-559`·`fh-973`, 전부 "신청하신 카드가 발급되어"로 시작한다). **신청한 적 없는 사람을 흔드는 것이 이 사기의 방식이라 신청했다는 말은 사기 쪽이 더 자주 한다** — 카드 배송 사칭에서 그 한 마디는 흉내가 아니라 문법이다. 같은 표지가 기관 자칭 게이트에서는 서고 여기서는 무너진다: **표지의 값은 신호마다 따로 재야 한다**
- [ ] **`familiar_person_claim` 은 호칭이 아니라 무엇을 세는가** — v1.1 `fh-925`·`fh-926`·`fh-930`, v1.2 `fh-1041`~`fh-1043`. 어휘가 **연락 수단이 바뀐 이유**로 좁혀져 있어서 "나야, 폰 바꿔서" 는 받고 "형 나야" 는 못 받는다. v1.3 이 `demand_gated_patterns` 로 **호칭 + 자기지목 + 요구**를 닫았고(`fh-1041` 포함), 남은 다섯 건은 **자기지목이 없다** — 호칭만으로는 진짜 가족과 갈리지 않으므로 다음 셋이 그 값을 치를 정상 짝을 먼저 넣어야 한다. **세 회차 연속 이월이다**
- [ ] **`money_transfer_request` 유형 공백** — v1.0 `fh-825`·v1.1 `fh-937` 에서 `보내 줘` + 계좌 목적지가 이 신호를 켜지 않는 것도 함께 봤다. 이 신호 단독에 대응하는 유형이 없는데 **스키마상 사례로 적을 수 없다**(`is_fraud` 는 유형 유무와 일치해야 한다). `fh-837` 이 원래 그 자리를 겨냥했다가 기관 자칭을 더해 돌렸다. **재려던 것을 재지 못하게 하는 스키마 제약도 결함이다**
- [x] **남은 어휘 공백** (2026-08-25, `7a06137`) — v1.2 가 14건으로 다시 쟀다(`fh-1035`~`fh-1048`): `credential` 의 공인인증서 **파일**·카드 **뒷면 세 자리**·**보안카드** 앞뒤, `money_mule` 의 **착오송금**·**잘못 보낸 돈**·**오입금**, `familiar_person_claim` 의 **호칭만으로 시작**, `loan_policy_offer` 의 **정책 자금**(띄어쓰기)·**청년 햇살론**, `card_delivery_claim` 의 **발급된 체크카드**·**택배 기사**, `money_transfer_request` 의 **옮겨**. v1.1 이 적어 둔 `fh-923`(끝난 일만 말하는 국세청 사칭)·`fh-902`(나열형 대상 + `-지 마십시오`, **두 층 사이로 빠진다**)는 아래로 옮겨 아직 열려 있다. **v1.3 이 열네 건을 전부 닫았다** — 어휘마다 값을 치를 정상 짝을 함께 넣었기 때문이다. **어휘 넓히기는 값을 정상 문장이 치르는 작업이라 그 짝이 없으면 넓힐 수 없다**는 것을 v1.2 는 회차가 끝날 때 알았고 v1.3 은 셋을 만들기 전에 알았다
- [ ] **`secrecy_isolation` 나열형 대상과 닫힌 절 안의 요구 대상** — v1.1 `fh-902`·`fh-906`·`fh-910`(나열형 대상 + `-지 마십시오`, **두 층 사이로 빠진다**), `fh-937`(완료 보고 절에 들어간 `안전계좌` — 닫힌 절이라 대상 탐색에서 사라진다), v1.1 `fh-923`(끝난 일만 말하는 국세청 사칭). **절 분해가 어휘보다 먼저다** — v1.3 이 세운 조건들은 전부 열린 절 위에서 돈다. 2~3회차 이월
- [x] **설명 실패에 이름을 붙였다** (2026-08-24) — `docs/34` 9절의 "안전 필터 차단율을 모른다" 와 "이 계층은 아무것도 기록하지 않는다" 는 **같은 항목이었다.** 기록할 게 없던 게 아니라 셀 것이 없었다 — 실패가 `None` 하나로 접혀 사유가 세 자리(client · `explain_analysis` · `explain_with_fallback`)에서 버려지고 있었다. 닫힌 어휘 20개(`ExplanationOutcome`)와 시도·결과 지표를 기존 `/internal/metrics` 에 붙였다. **사유를 문자열로 남기면 언젠가 본문이 그 문자열에 실린다** — provider 응답에서 온 문자열은 label 이 되지 않는다. 갈라 놓고 나서 9절이 걱정한 것이 `finishReason: SAFETY` 가 아니라 `promptFeedback.blockReason` 이었음도 드러났다. 상세는 `docs/34` 12절
- [ ] **분모를 채운다** — 위 항목은 **도구를 만든 것이지 답을 얻은 것이 아니다.** 유료 실행을 하지 않아 차단율의 분모가 0 이다. 한 번 돌려 실제 값을 `docs/34` 2절 실측 표 옆에 적어야 닫힌다. 예산 알림(`docs/31` 11-5)이 그보다 먼저다
- [ ] **설명 문장 품질 측정** — 열세 회차 동안 탐지만 쟀다. **근거 이탈률은 여전히 숫자가 없다.** 차단율과 달리 이건 계측만으로 안 되고 판정자가 필요하다 — 출력 검증이 거부한 건수(`rejected_*`)는 세지만, 통과한 문장이 근거에 붙어 있는지는 아무도 안 본다
- [ ] **v0.9 `fh-762`·`fh-764`·`fh-765` 라벨 재검토** — 카드 배송·투자 권유·사설 채널 안내가 조회 창구를 가리킨다고 본 쪽이 이 셋의 금지 라벨을 이겼다. **엔진이 아니라 라벨이 틀렸을 수 있다**
- [ ] **판정 프롬프트 v0.2 + 유료 재실행** — `FRAUD_JUDGE_PROMPT` 가 6종 taxonomy 위에 있어 이제 4종(`advance_fee_demand`·`investment_scheme`·`acquaintance_impersonation`·`isolation_coercion`)을 만들지 못한다. 프롬프트를 바꾸면 sha256 이 바뀌어 기존 판정 파일과의 연결이 끊긴다. **그 해시가 덮지 않는 자리도 하나 찾았다**(2026-08-23) — 프롬프트는 `ACTION_POLICIES` 의 **title** 을 `{actions}` 로 끼워 넣는데(`evaluation/llm_judge.py:236`) 기록된 `FRAUD_JUDGE_PROMPT_SHA256` 는 템플릿만의 해시다. title 을 고치면 유료 모델이 본 프롬프트가 바뀌는데 해시는 그대로 남는다. v0.2 에서 **해시가 끼워 넣은 title 까지 덮게** 고치고 `VERIFY_BY_KNOWN_CONTACT` 의 title 변경을 여기에 묶는다
- [ ] **투자 사기 공식 근거 확보** — 금융감독원 파인 제도권금융회사조회 URL 을 확인하지 못해 `investment_scheme` 액션이 `police_1394` 에 붙어 있다. 새 외부 참조는 검토 대상(`CLAUDE.md`)
- [ ] held-out v0.2 에 대한 LLM-only / Hybrid 3자 비교 (유료 호출 1회 필요. 현재 `not_run`)
- [x] URL lexical feature analysis (offline safe implementation)
- [ ] URL domain/reputation feature analysis (outbound-fetch policy required)
- [x] financial profile dashboard shell
- [x] financial profile frontend integration (backend CRUD v0.1)
- [x] deterministic profile metrics + live profile/Home status v0.1
- [x] official product detail / 2-product comparison UI
- [x] official product candidate UI (goal-only minimum input)
- [x] What-if loan simulation (backend-only calculation, current vs alternative UI)
- [x] 재테크 기초 가이드 v0.1 (공식 금융교육 근거, 입력·종목·매매 추천 없음)
- [x] API latency instrumentation (exact JSON duration + process histogram; dashboard pending)
- [x] privacy-safe request logging / PII non-disclosure regression
- [x] frontend accessibility v0.1 (skip link, focus ring, live status, reduced motion, structural regression)
- [x] responsive dark-browser check (375/768/1280, nav transition, horizontal overflow)
- [ ] screen reader + quantitative AA contrast + light mode + iOS Safari device audit
- [x] PWA 설치 + 문자 앱 공유 시트 진입 (`share_target`은 원문을 주소에 싣지 않도록 POST, 오프라인 셸, 설치 유도) — 실기기 공유 확인은 실도메인 이후
- [ ] account-level audit log (requires identity and retention policy)

## P2 — 본선/Stretch
- [ ] STT
- [ ] voice scam scenario
- [ ] AI red-team suite
- [x] prompt injection benchmark (2026-08-20, 7건 개발셋) — `evaluation/data/injection_golden_v0.1.jsonl`, `tests/test_llm_prompt_injection.py`. **held-out 아니고 방어 교정에 쓴 셋이라 통과율을 방어 성능으로 주장하지 않는다**
- [ ] screenshot/phishing-page analysis
- [ ] advanced personalization
