# Fraud Evaluation Benchmark v0.1

## 목적

Fraud Scenario Engine을 단순 정확도가 아니라 탐지, 유형, 필수 신호, 사용자 상태별
행동, 공식 근거 연결로 나눠 재현 가능하게 측정한다.

## 데이터

- ID: `fraud_golden_v0.1`
- 61건: 위험 39건, 정상·예방 문맥 22건
- 사회초년생, 소상공인, 미지정 persona와 7개 `UserState` 포함
- 모두 팀이 직접 작성한 합성 문장
- 개인정보·실제 피해 메시지·외부 데이터셋 없음
- bootstrap 개발셋이며 `held_out: false`
- 결과 JSON에 persona·state 분포와 정규화 데이터 SHA-256 포함
- 모든 `UserState` 최소 3건, 단 `received_only` 43건으로 불균형

상세 출처와 라벨 계약은 `evaluation/data/README.md`에 있다. 이 데이터는 엔진 교정에도
사용됐으므로 실서비스 일반화 성능의 근거가 아니다.

## 재현

```bash
python -m scripts.evaluate_fraud_engine --check
python -m scripts.evaluate_fraud_engine \
  --check \
  --performance-repeats 20 \
  --output evaluation/results/fraud-benchmark-v0.1.json
```

`--check`는 Scenario Engine의 precision 0.75 이상, recall 0.65 이상, FPR 0.25 이하,
필수 signal·action coverage 및 상태 정책 정확도 0.90 이상, 공식 근거 coverage 1.0을
요구한다. CI도 동일 명령을 별도 실행한다.

LLM 단독 판정은 **별도 스크립트**가 만든다. 위 명령은 CI가 매 푸시마다 돌리므로
네트워크도 비용도 없어야 하고, 유료 호출이 거기서 나가면 안 된다.

```bash
FINSHIELD_LLM_PROVIDER=google_ai_studio \
GEMINI_API_KEY_FILE=secrets/gemini_api_key.txt \
python -m scripts.run_llm_fraud_judge --concurrency 5
```

결과는 `evaluation/results/llm-judge-fraud-v0.1.json`에 판정 61건 그대로 저장되고
저장소에 커밋된다. 그래야 CI와 심사자가 유료 호출 없이 같은 표를 다시 만들 수 있고,
어떤 데이터·모델·프롬프트로 잰 숫자인지 파일 하나로 확인된다. 판정 결과에는 골든셋의
SHA-256이 함께 적히며, 골든셋을 고치고 재측정하지 않으면 `llm_only.status`가
`stale`이 되고 `--check`가 실패한다. 비교를 포기할 때는 결과 파일을 지운다.

## 2026-08-13 측정 결과

| 항목 | Legacy 5-keyword baseline | Scenario Engine v0.1 |
|---|---:|---:|
| Precision | 0.857143 | 0.973684 |
| Recall | 0.461538 | 0.948718 |
| F1 | 0.600000 | 0.961039 |
| FPR | 0.136364 | 0.045455 |
| Accuracy | 0.606557 | 0.950820 |

Scenario Engine의 필수 signal coverage는 0.943396, 필수 action coverage는 0.966667,
상태 정책 정확도는 0.967213, action-source 근거 coverage는 1.0이다. 상세 유형별
수치와 오류 ID는 `evaluation/results/fraud-benchmark-v0.1.json`을 기준으로 한다.

> 이 표는 2026-08-13 시점 기록으로 남겨 둔다. Scenario Engine 숫자는 2026-08-19
> 어휘 확장 v0.2로 바뀌었다(아래 "어휘 확장 v0.2 재측정"). 결과 JSON은 항상 최신
> 실행을 담으므로, 이 표와 JSON이 다르면 JSON이 맞다.

## 알려진 오류

- ~~false negative: `fg-046`, `fg-047`~~ — 2026-08-19 어휘 확장 v0.2로 닫혔다.
- ~~false positive: `fg-049`~~ — 2026-08-19 억제 규칙 추가로 닫혔다.
- 기관 사칭 유형 support가 6건에 불과해 유형별 수치의 불확실성이 크다.
- 예방 문맥 억제는 일반 자연어 부정 해석기가 아니라 명시적 안전 문구에 대한 좁은
  규칙이다. 안전 문구 뒤에 직접 요구가 이어지면 억제하지 않는다.
- **남아 있는 신호 누락 1건**: `fg-006` "정책자금 대상자로 선정됐습니다. 보증료 입금
  후 실행됩니다."가 `money_transfer_request`를 켜지 못한다(`loan_policy_offer`만
  켜진다). 이진 판정은 맞으므로 F1에는 나타나지 않고, 필수 signal coverage에만
  52/53 = 0.981132로 남는다. 닫지 않은 이유는 아래 "닫지 않기로 한 것"에 적었다.

오류 ID를 지우거나 점수만 높이기 위해 데이터에서 제거하지 않는다. v0.2 held-out
평가 전에 유형별 표본과 변형 문장을 늘린다.

**2026-08-19 추가.** LLM 단독 판정이 `fg-046`·`fg-047`을 둘 다 잡았다(아래 3자 비교).
두 문장은 사칭 기관("법원 집행 담당")과 설치 유도("보안 모듈을 내려받아 실행")를
현재 규칙 어휘가 담지 못해 놓친 것이므로, v0.2에서 신호 어휘를 넓힐 대상이다. 모델을
판정에 넣는 것이 아니라 **모델이 찾아 준 구멍을 규칙으로 메우는** 방향이다.

## 2026-08-19 Rule / LLM / Hybrid 3자 비교

`CLAUDE.md`의 Evaluation 절이 요구하는 비교다. 같은 골든셋 61건, 같은 입력
(원문·persona·state), 같은 출력 어휘(사기 유형 6개, 위험 수준 3단계, 행동 코드 11개)를
주고 `gemini-3.6-flash` 하나에게 엔진의 일을 통째로 시켰다. 판정자는
`evaluation/llm_judge.py`에 있고 **제품에는 없다** — 비교를 위해 일부러 만든 경로다.

| 항목 | Legacy 5-keyword | Scenario Engine v0.1 | LLM-only | Hybrid v0.1 (배포본) |
|---|---:|---:|---:|---:|
| Precision | 0.857143 | 0.973684 | 0.951220 | 0.973684 |
| Recall | 0.461538 | 0.948718 | **1.000000** | 0.948718 |
| F1 | 0.600000 | 0.961039 | **0.975000** | 0.961039 |
| FPR | 0.136364 | **0.045455** | 0.090909 | **0.045455** |
| Accuracy | 0.606557 | 0.950820 | 0.967213 | 0.950820 |
| 필수 행동 coverage | – | **0.966667** | 0.600000 | **0.966667** |
| 상태 정책 정확도 | – | **0.967213** | 0.508197 | **0.967213** |
| 공식 근거 coverage | – | **1.0** | 0.0 | **1.0** |

61건 전부 응답했고(실패 0건), 어휘 밖 코드를 만들어 낸 경우도 0건이다. 판정 지연은
p50 2.21초, p95 5.41초다.

> 이 표의 Scenario Engine·Hybrid 열은 **비교를 수행한 시점(v0.1)의 값**이다. 같은 날
> 이 비교 결과를 반영해 어휘를 넓혔고, 그 뒤 숫자는 아래 "어휘 확장 v0.2 재측정"에
> 있다. LLM-only 열은 유료 판정 파일에 고정돼 있어 바뀌지 않는다.

**읽는 법이 중요하다. 모델이 탐지에서 우리 엔진을 이겼다.** 재현율 1.000으로 엔진이
놓친 `fg-046`(법원 집행관 사칭)·`fg-047`(보안 모듈 설치 유도)을 둘 다 잡았다. 이것을
숨기면 이 표는 증거가 아니라 홍보물이 된다.

그다음 세 줄이 이 제품이 규칙에 판정 권한을 남겨 둔 이유다.

- **필수 행동 0.600.** 30건에서 필요한 행동이 빠졌다. 가장 많이 빠진 것은
  `CONTACT_KISA_118`(14건), `PRESERVE_EVIDENCE`(10건),
  `CONTACT_FINANCIAL_INSTITUTION`(7건)이다. "이건 사기예요"까지는 맞히고
  "그래서 지금 뭘 하세요"에서 무너진다. 사용자에게 필요한 쪽은 뒤쪽이다.
- **상태 정책 0.508.** 메시지가 평범해도 이미 앱을 설치했거나 송금한 상태는
  그 자체로 높은 위험이라는 것이 우리 정책인데(`STATE_MINIMUM_RISK`), 모델은 9건에서
  메시지만 보고 `low`로 내렸다. 정책표를 프롬프트에 적어 지키기를 바라는 것보다
  조회표가 낫다는 근거다. 다만 공정하게 적어 두면, 모델에게 이 정책표 자체를 준 적은
  없다. 준다 해도 지킴 여부가 확률적이라는 점이 요지다.
- **공식 근거 0.0.** 측정 실패가 아니라 구조다. 모델은 검증 가능한 출처 ID를 만들지
  않고, 만들게 하면 그것이 곧 지어낸 근거다. 이 항목은 규칙 경로만 채울 수 있다.

모델이 낸 오탐 2건도 같은 성격이다. `fg-054`(가족에게 계좌 접근수단을 건넸다)와
`fg-058`(모르는 입금을 확인만 했다)은 **메시지에 사기 문구가 없지만 상황은 위험한**
사례다. 엔진은 "사기 메시지는 아니다"라고 판정하면서 상태 정책으로 긴급 행동을 내보내
둘을 분리하는데, 모델은 둘을 한 덩어리로 보고 `is_fraud: true`를 냈다.

### 조합해 봤지만 넣지 않은 것

같은 예측 벡터로 계산한 것이라 추가 비용은 없다. 결과 JSON의
`hybrid_v0_1.alternatives_considered`에 있다.

| 조합 | Precision | Recall | F1 | FPR |
|---|---:|---:|---:|---:|
| 규칙 ∪ 모델 (어느 쪽이든 걸리면 경고) | 0.928571 | 1.000000 | 0.962963 | 0.136364 |
| 규칙 ∩ 모델 (둘 다 동의할 때만 경고) | **1.000000** | 0.948718 | 0.973684 | **0.000000** |

교집합은 오탐 0에 정밀도 1.0으로 표에서 가장 예쁘다. 그런데도 넣지 않았다. 두 조합
모두 모델이 최종 경고 여부를 움직이게 하므로 `CLAUDE.md`의 첫 non-negotiable을 어기고,
교집합은 특히 **모델이 죽으면 경고가 사라지는** 구조다 — 지금 위험한 문자를 받은
사람에게 외부 API 장애가 곧 무경고가 되는 설계다. 숫자만 보고 고를 수 있는 자리가
아니라서 숫자와 함께 이유를 남긴다.

### 이 비교가 증명하지 않는 것

- 프롬프트 한 벌, 모델 한 개, 실행 한 번의 결과다. 다른 프롬프트로 재면 달라진다.
- 골든셋 61건은 엔진 교정에 사용된 개발셋이다. 규칙 쪽에 유리한 방향의 편향이며,
  그럼에도 탐지에서 모델이 앞섰다는 점이 위 결과의 의미를 오히려 강화한다.
  (**2026-08-19 추가**: 같은 날 어휘 확장으로 엔진이 이 셋에서 만점을 받았다. 그런데
  이건 "엔진이 모델을 이겼다"가 아니다. 모델은 이 셋을 처음 봤고 엔진은 이 셋으로
  다듬어졌다. 두 숫자는 이제 같은 조건의 비교가 아니며, 다시 비교하려면 held-out
  셋에서 둘 다 다시 재야 한다.)
- 설명 문장의 품질(근거 이탈률, 안전 필터 차단율)은 여기서 재지 않았다. 위 숫자는
  전부 탐지·행동에 대한 것이다.

## 2026-08-19 어휘 확장 v0.2 재측정

위 비교가 찾아 준 구멍을 규칙으로 메웠다. 바꾼 것은 `app/domain/fraud/signals.py`
한 파일이고, 판정 로직·가중치·상태 정책은 건드리지 않았다. 어휘와 억제 문구만 넓혔다.

| 신호 | 추가한 어휘 | 이유 |
|---|---|---|
| `authority_impersonation` | `법원`, `집행관` | `fg-046`. 실제 사칭에 쓰이는 기관인데 목록에 없었다 |
| `app_install_request` | `내려받`, `다운로드`, `설치 파일`, `설치 링크` | `fg-047`. 기존 어휘가 전부 "설치"라는 단어에 걸려 있었다 |
| `money_transfer_request` | `이체하세요`, `이체해`, `이체 바랍니다`, `이체를 진행` | "송금"의 동의어가 통째로 빠져 있었다. 지시형만 넣었다 |

여기에 억제 규칙 하나를 더했다. `공식 누리집`·`공식 대표번호` 같은 **공식 창구 확인
안내** 문구가 있으면서 읽는 사람에게 요구하는 것이 하나도 없으면, 기관명·상품명만으로
켜지는 신호(`authority_impersonation`, `loan_policy_offer`)를 끈다. 사기 문자는 공식
창구로 가라고 말하지 않는다 — 가면 거짓이 드러나기 때문이다. 요구 문구가 하나라도
있으면 억제하지 않으므로, 안전 문구를 앞에 붙여 놓고 뒤에서 요구하는 혼합 문장은
그대로 걸린다(`tests/test_fraud_scenario.py`에 두 방향 모두 고정했다).

| 항목 | Scenario Engine v0.1 | **Scenario Engine v0.2** | LLM-only |
|---|---:|---:|---:|
| Precision | 0.973684 | **1.000000** | 0.951220 |
| Recall | 0.948718 | **1.000000** | 1.000000 |
| F1 | 0.961039 | **1.000000** | 0.975000 |
| FPR | 0.045455 | **0.000000** | 0.090909 |
| Accuracy | 0.950820 | **1.000000** | 0.967213 |
| 필수 signal coverage | 0.943396 | 0.981132 | – |
| 필수 action coverage | 0.966667 | **1.000000** | 0.600000 |
| 상태 정책 정확도 | 0.967213 | **1.000000** | 0.508197 |
| 공식 근거 coverage | 1.0 | 1.0 | 0.0 |

### 이 1.0을 어떻게 읽어야 하는가

**성능이 완벽해진 것이 아니라 개발셋이 변별력을 잃은 것이다.** 골든셋 61건은 엔진을
교정하면서 만든 셋이고, 그 셋으로 다시 재서 만점이 나왔다. 남은 정보량이 0에 가깝다는
뜻이지 일반화의 증거가 아니다. 다음 측정은 이 셋으로 하면 안 된다.

세 오류의 성격도 같지 않다. 나눠서 봐야 한다.

- `fg-046`·`fg-047`(false negative)은 **엔진을 본 적 없는 외부 판정자**가 찾아 줬다.
  우리가 못 보던 구멍이라는 증거로서는 그나마 독립적이다.
- `fg-049`(false positive)는 **우리가 이미 보고 있던 실패 케이스**를 겨냥해 억제
  규칙을 설계한 것이다. 이건 교정(calibration)이지 일반화가 아니다. 억제 규칙이
  `fg-049` 한 건에 맞춰 깎인 것은 아닌지 held-out 셋에서 다시 확인해야 한다.

어휘를 넓히는 변경은 골든셋 안에서는 좋아 보이면서 밖에서 오탐을 늘리기 쉽고,
골든셋으로 재면 그 손해가 보이지 않는다. 그래서 골든셋에 **없는** 문장 14개
(정상 10 + 사기 4)로 따로 확인했고, 실제로 이 변경이 만든 오탐 6건을 찾아 되돌렸다.
그 문장들은 `tests/test_fraud_scenario.py`에 회귀 테스트로 고정했다.

### 닫지 않기로 한 것

- **국세청·세무서·건강보험공단·국민연금공단·금융위원회·예금보험공사.** 사칭이 잦은
  기관이라 함께 넣어 봤다. 골든셋 점수는 그대로였고, 골든셋 밖 정상 문장("국세청
  홈택스에서 연말정산 자료를 조회할 수 있습니다")에서 오탐이 5건 났다. 되돌렸다.
  **측정이 요구하지 않은 어휘는 넣지 않는다.** 넣으려면 held-out 셋으로 오탐을 재고
  넣는다.
- **`fg-006`의 `money_transfer_request`.** "보증료 입금 후 실행됩니다"는 명령형이
  아니라 조건절이다. 맨 `입금`을 넣으면 "입금 후 2영업일 이내 반영됩니다" 같은 정상
  안내가 걸린다. 이 케이스는 `loan_policy_offer`로 이미 사기로 판정되므로 탐지
  손해는 없고, 필수 signal coverage에만 남는다. 어휘를 늘리는 대신 held-out 셋에
  같은 형태의 변형 문장을 넣어 실제 빈도를 먼저 재기로 한다.

### 이 변경이 만들지 않은 것

`법원 등기 우편물 도착 예정 안내입니다`는 `authority_impersonation`을 켠다.
`법원`을 넣어서 생긴 문제로 보이지만 아니다. 기존 어휘로도 `금융감독원 보도자료가
게시되었습니다`, `경찰 교통민원 안내입니다`가 똑같이 켜진다. 기관명이 나오면 medium
으로 올리고 "공식 창구로 직접 확인하라"고 말하는 것은 원래부터의 설계다. v0.2가
바꾼 것이 아니므로 여기서 함께 고치지 않고, held-out 평가에서 이 설계 자체가 맞는지
다시 판단한다.

## Baseline 정직성

- `legacy_rule_v0`: 실행됨. 기존 공개 호환 5-keyword 규칙이다.
- `scenario_engine_v0_1`: 실행됨. 결정론적 signal·taxonomy·state policy·evidence다.
- `llm_only`: 2026-08-19 실행됨. `gemini-3.6-flash`, `fraud_judge_v1` 프롬프트,
  temperature 0.0, 61건 전건. 계약과 실행 정보는 결과 JSON의 `llm_only.contract`와
  `llm_only.run`에 있다.
- `hybrid_v0_1`: 실행됨. 배포된 조합이며 탐지 숫자는 `scenario_engine_v0_1`과
  **동일하다.** 설명 계층이 `AnalyzeResponse`를 받아 문자열을 돌려주므로 위험 수준·
  유형·행동을 구조적으로 바꿀 수 없기 때문이다. 두 숫자가 갈라지는 날은 그 경계가
  깨진 날이다.

품질 게이트는 엔진 숫자만 본다. 모델 쪽에 기준선을 걸면 우리가 고칠 수 없는 이유로
CI가 실패한다. 대신 "낡은 비교"만 막는다(`llm_only_stale`).

따라서 이 결과로 “Hybrid가 LLM-only보다 탐지 성능이 좋다”고 주장하지 않는다. v0.1
시점에는 탐지만 보면 LLM-only가 더 나았고, v0.2 이후로는 엔진이 이 셋에서 만점이지만
그건 엔진이 이 셋으로 교정됐기 때문이라 비교 근거로 쓸 수 없다. 어느 쪽이든 주장하는
것은 다른 것이다 — **행동과 근거까지 포함한 제품 출력 전체에서는 결정론 경로만 채울
수 있는 칸이 있고, 그 칸이 사용자가 실제로 필요로 하는 칸이다.** 이 칸에 대한 격차
(행동 coverage 0.600, 상태 정책 0.508, 근거 coverage 0.0)는 어휘 확장과 무관하게
그대로다.

## 2026-08-20 held-out v0.2 첫 측정

`evaluation/data/fraud_holdout_v0.2.jsonl` 72건(사기 44 · 정상 28).
결과는 `evaluation/results/fraud-holdout-v0.2.json`,
데이터 sha256 `5ddb3904d15a4c35231819317391a498eef373f6995bbbf1e9b1ea4d0c373d47`.

```bash
python -m scripts.evaluate_fraud_engine \
  --dataset evaluation/data/fraud_holdout_v0.2.jsonl \
  --output evaluation/results/fraud-holdout-v0.2.json
```

`--check` 를 붙이지 않는다. 이 셋에 게이트를 걸면 빨개질 때마다 이 셋을 보고 규칙을
고치게 되고, 그러면 held-out 이 아니게 된다.

### 개발셋과의 차이

| | 개발셋 v0.1 (61건) | held-out v0.2 (72건) |
|---|---|---|
| precision | 1.000 | **0.854** |
| recall | 1.000 | **0.795** |
| f1 | 1.000 | **0.824** |
| 오탐률 | 0.000 | **0.214** |
| 정확도 | 1.000 | **0.792** |
| 필수 signal coverage | 0.981 | **0.836** |
| 상태 정책 정확도 | 1.000 | **0.681** |
| 필수 action coverage | 1.000 | 0.933 |
| 공식 근거 coverage | 1.000 | 1.000 |

**개발셋 만점이 성능이 아니었다는 것이 숫자로 확인됐다.** 상태 정책 정확도가 1.000
에서 0.681 로 떨어진 것이 가장 크다. 이 지표는 "위험 등급이 라벨 이상이고 필요한
행동이 모두 나왔는가" 를 사례 단위로 보는 것이라, 하나라도 어긋나면 0 이다.

품질 게이트 기준선(precision 0.75 / recall 0.65 / FPR 0.25)은 held-out 에서도
넘는다. 넘지 못하는 것은 상태 정책 정확도(0.681 < 0.90)와 signal coverage
(0.836 < 0.90)다.

### 미탐 9건 — 원인은 어휘가 아니라 세 가지다

**(1) 금융기관·행정기관 사칭 어휘가 통째로 없다** — `fh-004`(건강보험공단),
`fh-005`(국세청), `fh-007`(병무청), `fh-009`(국민은행), `fh-010`(카카오뱅크),
`fh-011`(신한카드). 6건 전부 신호 0개다. `authority_impersonation` 재현율이
0.333(12건 중 4건)까지 떨어진 직접적인 이유다.

이 공백은 **알고 만든 것**이다. 위 「닫지 않기로 한 것」에서 국세청·건강보험공단을
넣었다가 정상 문장 오탐 5건을 보고 되돌렸고, "넣으려면 held-out 셋으로 오탐을 재고
넣는다" 고 적었다. 이제 그 셋이 생겼다. 다만 맨 기관명을 넣는 방식은 여전히 틀렸다
— 같은 셋의 `fh-052`(국세청 홈택스 정상 안내)·`fh-053`(건강보험료 정상 고지)이
그대로 오탐이 된다. 필요한 것은 기관명이 아니라 **자칭 형태**(`…입니다`)와 요구의
결합이다.

**(2) 유형 표에 `money_transfer_request` 로 가는 길이 없다** — `fh-017`
("작업비를 먼저 송금해 주시면"). 신호는 켜졌고 위험 등급도 `high` 로 올라갔는데
`fraud_types` 가 비어서 미탐으로 집계됐다. `classify_fraud_types` 에 이 코드로
들어오는 분기가 없다. 어휘 문제가 아니라 **분류 표의 구멍**이고, 사용자에게는
"위험은 높은데 무슨 사기인지는 말 못 하는" 출력으로 보인다.

**(3) 어미 활용을 못 넘는다** — `fh-031`. 어휘에는 `제3자 계좌로 옮기` 가 있고
문장은 `제3자 계좌로 옮겨` 다. 부분 문자열 매칭이라 한 글자에 걸려 통째로 빠진다.

`fh-040`(주소 없는 청첩장 링크 유도)은 라벨 단계에서 필수 신호를 비워 둔 경계
사례다. 본문만으로 정상 청첩장과 구분되지 않으므로 이 미탐은 결함으로 세지 않는다.

### 오탐 6건 — 전부 같은 원인이다

| 사례 | 켜진 신호 | 걸린 어휘 |
|---|---|---|
| `fh-050` 본인이 신청한 카드 재발급 배송 | `account_access` | `체크카드` |
| `fh-060` 안전계좌는 없다는 예방 안내 | `authority_impersonation`·`money_transfer_request` | `수사기관`·`안전계좌` |
| `fh-063` 중고거래 "주소 다시 보내 주세요" | `money_mule` | `다시 보내` |
| `fh-064` 사내 "영수증을 전달해 주세요" | `money_mule` | `전달해` |
| `fh-068` "은행 앱 설치했는데 로그인이 안 돼요" | `remote_app` | `앱 설치` |
| `fh-072` "창구에서 계좌 비밀번호 변경했습니다" | `credential` | `비밀번호` |

**여섯 건 모두 명사·동사가 문장에 등장했다는 사실만으로 켜졌다.** 누가 누구에게
무엇을 요구하는지는 보지 않는다. `fh-068`·`fh-072` 는 특히 나쁘다 — 사용자가
**자기가 한 일을 설명하는 문장**이 사기 판정을 받는다. 이 제품에서 그런 문장은
정상 입력이 아니라 **가장 흔한 입력**이다.

`fh-060` 은 억제 규칙이 한 글자 차이로 빗나간 경우다. 억제 조건은
`요구하지 않습니다` 인데 문장은 `이런 요구를 받으면` 이다.

### 무엇을 하지 않았나

**이 측정으로 규칙을 고치지 않았다.** 고치는 순간 v0.2 는 held-out 이 아니게 되고,
같은 셋으로 잰 다음 숫자는 다시 기억이 된다. 위 표는 이 셋의 **유일한 깨끗한
측정**이며 그대로 남는다.

수정은 별도 변경으로 하고, 그 시점에 v0.2 를 "소진됨" 으로 표시한다. 다음 깨끗한
측정에는 v0.3 이 필요하다.

> **2026-08-20: 소진됨.** 아래 「엔진 수정 v0.3」에서 이 셋을 보고 규칙을 고쳤다.
> 이후 v0.2 숫자는 성능이 아니라 기억이다. **위 표가 이 셋의 유일한 깨끗한
> 측정이며 그대로 남는다.**

### 이 셋이 아직 재지 못하는 것

- **투자·리딩방 사기 유형이 없다.** 유형 어휘가 6종으로 고정돼 있어 라벨을 붙일 수
  없었다. 실제 피해 규모로는 상위권인데 분류 표에 자리가 없다는 뜻이고, 이건 데이터가
  아니라 taxonomy 의 공백이다. → **2026-08-21 닫힘**(「taxonomy 확장 v0.4」).
  v0.2 셋 자체는 이 유형을 담지 않으므로 위 표는 그대로 남는다.
- LLM 단독 판정은 돌리지 않았다(`llm_only.status = not_run`). 이 셋에 대한 3자 비교는
  유료 호출을 한 번 더 해야 나온다.
- 합성이고 저자가 엔진 어휘를 알고 있다. 한계는 `evaluation/data/README.md` 에 있다.

## 2026-08-20 엔진 수정 v0.3 과 held-out v0.3 재측정

held-out v0.2 가 드러낸 네 결함을 고쳤다. 고치는 순간 v0.2 는 **소진**된다 — 그 셋을
보고 규칙을 고쳤으므로 이후 v0.2 숫자는 성능이 아니라 기억이다. 그래서 수정 **전에**
새 held-out v0.3 을 얼려 커밋했다.

### 커밋 순서가 증거다

| 커밋 | 내용 |
|---|---|
| `570d04b` | taxonomy 에 `advance_fee_demand` 추가 (엔진 변경 없음) |
| `0508408` | v0.3 셋 60건 동결 + **수정 전 baseline 측정** |
| `c7ad752` | 엔진 수정 |
| 이 문서 | 수정 후 재측정 |

`fraud-holdout-v0.3-before-fix.json` 은 엔진 수정이 존재하기 전에 커밋됐다. 사후에
"원래는 이랬다" 고 주장하는 것이 아니라 기록에서 확인된다.

```bash
python -m scripts.evaluate_fraud_engine \
  --dataset evaluation/data/fraud_holdout_v0.3.jsonl \
  --output evaluation/results/fraud-holdout-v0.3.json
```

데이터 sha256 `8a36273d7edcca57d13224ebee7c1af5ebd6d84093212b75ade848d6abfd9aab`,
60건(사기 31 · 정상 29). 여기에도 `--check` 를 붙이지 않는다.

### 고친 네 가지

| # | v0.2 가 드러낸 결함 | 수정 |
|---|---|---|
| 1 | 은행·기관 자칭 어휘 부재 (`authority_impersonation` 재현율 0.333) | 어휘를 두 층으로 나눴다. 수사기관·법원은 언급만으로, 은행·카드사·공단은 **요구가 함께 있을 때만** |
| 2 | `money_transfer_request` 가 유형 표에 없음 (등급 high 인데 유형 빔) | `advance_fee_demand` 유형 신설. 송금 요구 + 선입금 표지 |
| 3 | 한국어 어미를 못 넘는 부분 문자열 매칭 | 필요한 활용형을 적되 목적어 조건을 함께 걸었다 |
| 4 | 요구 맥락 없이 맨 명사만으로 켜짐 (오탐 6건 전부) | 요구 조건부·금액 조건부 어휘 도입, 예방 서술은 절 단위로 제외 |

2번에는 요약 문장 수정이 딸려 있다. 유형을 못 붙이면 요약이 "신호가 확인되지
않았습니다" 라고 말해서, 같은 응답 안에서 위험 등급과 요약이 서로를 부정했다.
이제 "유형을 특정하지는 못했지만 …신호가 확인되었습니다" 로 나간다.

4번의 요구 판정은 **두 번 만에 맞췄다.** 처음에는 맨 명령형 어미("하세요",
"바랍니다")를 요구로 셌더니 "이체 내역을 확인하세요. 모르는 출금이 있으면 은행에
문의하세요" 가 은행 사칭이 됐다. 정상 안내도 명령형을 쓴다. 요구를 만드는 것은
어미가 아니라 **무엇을 달라고 하는가**이므로, 위험한 행동이 동사가 된 자리
("설치하세요")와 넘겨줄 수 있는 물건에 대한 필요 서술("체크카드가 필요합니다")만
센다. 부정형("설치하지 마세요")과 완료 통보("인증이 완료되었습니다")는 뺀다.

### 수정 전 / 수정 후 (held-out v0.3, 같은 셋)

| 항목 | 수정 전 | **수정 후** |
|---|---:|---:|
| Precision | 0.677419 | **1.000000** |
| Recall | 0.677419 | **0.967742** |
| F1 | 0.677419 | **0.983607** |
| FPR | 0.344828 | **0.000000** |
| Accuracy | 0.666667 | **0.983333** |
| 필수 signal coverage | 0.735849 | **0.981132** |
| 필수 action coverage | 0.920732 | **1.000000** |
| 상태 정책 정확도 | 0.750000 | 0.833333 |
| 공식 근거 coverage | 1.0 | 1.0 |

오탐 10건 → **0건**, 미탐 10건 → **1건**(`fh-138`).

### 이 숫자를 어떻게 읽어야 하는가

**일반화 성능이 아니다.** v0.3 은 v0.2 가 드러낸 네 결함을 겨냥해 만든
**표적 회귀 셋**이고, 셋과 수정을 같은 사람이 설계했다. 이 표가 보증하는 것은
"고치겠다고 한 것을 고쳤고 그 과정에서 다른 것을 부수지 않았다" 까지다. 처음 보는
분포에서 어떻게 나오는지는 **이 표가 답하지 않는다.** v0.4 가 있어야 한다.

남은 오류 두 종류도 성격이 다르다.

- **`fh-138` (미탐 1건) 은 일부러 남긴 것이다.** "넘겨 주세요" 를 쓰는 자금 재전달
  사례인데, 어휘에 `넘겨` 를 넣으면 이 한 건이 닫히면서 **측정 자체가 무너진다.**
  재는 셋을 보고 어휘를 넣으면 그 셋은 더 이상 held-out 이 아니다. 어휘 확장으로
  닫을 수 있는 구멍이라는 사실 자체가 기록으로 더 쓸모 있다.
- **상태 정책 0.833 의 10건은 탐지가 아니라 위험 정책의 공백이다.**
  `fh-102`·`103`·`104`·`105`·`110`·`139`·`140`·`141`·`157`·`159` 는 라벨이 `high`
  인데 엔진이 `medium` 을 낸다. `HIGH_RISK_SIGNAL_COMBINATIONS` 에 사칭+요구 조합이
  두 쌍(`authority+money_transfer`, `loan+account_access`)밖에 없기 때문이다.
  **이번에 함께 고치지 않았다.** 등급 정책까지 같이 움직이면 위 표의 변화가 탐지
  수정 때문인지 등급 수정 때문인지 갈라낼 수 없다. 별도 항목으로 남겼다.

### held-out v0.2 는 이제 소진됐다

같은 수정 뒤 v0.2 를 다시 재면 f1 0.824 → 0.955, FPR 0.214 → 0.071 이다.
**이 숫자를 성능 근거로 쓰지 않는다.** 이 셋을 보고 고쳤기 때문이다. 결과 파일은
갱신해 두되(`fraud-holdout-v0.2.json`) 위 「2026-08-20 held-out v0.2 첫 측정」의
표가 이 셋의 유일한 깨끗한 측정으로 남는다.

그럼에도 **소진된 셋이 여전히 알려 주는 것이 있다.** v0.2 에 남은 오탐 2건은
v0.3 에 없는 형태다.

| 사례 | 왜 아직 켜지는가 |
|---|---|
| `fh-050` "체크카드 재발급 발송… 공식 앱에서 **등록해 주세요**" | 요구는 있는데 그 요구의 대상이 체크카드가 아니다 |
| `fh-063` "**입금** 확인했습니다… 주소가 바뀌었으면 **다시 보내** 주세요" | 돈이 언급되지만 '다시 보내'의 목적어는 주소다 |

요구·금액 조건을 **메시지 단위**로 걸었기 때문이다. 요구와 대상을 절 단위로 묶으면
닫히지만, 그 변경은 v0.4 를 얼린 뒤에 한다. 지금 v0.2 를 보고 고치면 그 두 건에
맞춰 깎는 것이고, v0.3 은 이 형태를 담고 있지 않아 손해를 보여 주지 못한다.
**v0.3 의 FPR 0.000 이 오탐을 다 없앴다는 뜻이 아니라는 근거가 바로 이것이다.**

미탐 `fh-004`(건강보험공단 자칭 + "본인인증 후 신청하지 않으면 소멸됩니다")도 남아
있다. 위협형 조건절이 요구로 세어지지 않는다. `fh-040`(청첩장)은 라벨 단계의 경계
사례라 결함으로 세지 않는다.

### 개발셋 v0.1 과 주입 골든셋

v0.1 은 만점을 유지한다(f1 1.000, 상태 정책 1.000). 변별력이 없는 셋이므로 이 값은
**회귀 확인용**이지 성능 근거가 아니다. Legacy 5-keyword 기준선은 f1 0.600 → 0.623,
FPR 0.136 으로 거의 그대로다 — 요구 조건이 이쪽에도 적용되면서 오탐이 줄고 미탐이
한 건 옮겨 갔다.

주입 골든셋의 `pi-002`("국민은행입니다. 계좌 확인을 위해 링크를 눌러 주세요…")는
결정론 판정이 `low` → `medium` 으로 올라갔다. 이 `low` 는 원래 주입과 무관한
**탐지 공백**이었고 이 문서 소관으로 넘어와 있었다. 1번 수정이 닫았으므로 골든
레이블을 고쳤다. 그 셋의 `expected_min_risk` 는 주입 방어가 아니라 탐지 성능을
기록한다는 점을 함께 적어 두었다.

### 다음에 필요한 것

- **held-out v0.4.** 위 표가 답하지 못하는 일반화 질문용. 절 단위 요구·대상 결합을
  고치기 **전에** 얼려야 한다.
- **`HIGH_RISK_SIGNAL_COMBINATIONS` 확장.** 사칭·제안 + 요구를 더 넓게 덮는다.
  v0.3 의 10건이 그 대상이다.
- **판정 프롬프트 v0.2 와 유료 재실행.** `FRAUD_JUDGE_PROMPT` 는 아직 6종 taxonomy
  위에 있어 `advance_fee_demand` 를 만들지 못한다. 프롬프트를 바꾸면 sha256 이
  바뀌므로 기존 판정 파일과의 연결이 끊긴다. 버전을 올려 다시 돌려야 3자 비교가
  새 유형을 포함한다.
- **투자·리딩방 / 지인 사칭 taxonomy.** 여전히 자리가 없다.
  → **2026-08-21 닫힘.** 아래 절 참조.

## 2026-08-21 taxonomy 확장 v0.4 와 held-out v0.4

`investment_scheme` 과 `acquaintance_impersonation` 을 유형 표에 넣고 탐지를
구현했다. 유형은 이제 9종이다. v0.3 에서 검증된 4-커밋 순서를 그대로 반복했다.

| 커밋 | 내용 |
|---|---|
| `30563dd` | taxonomy 에 두 유형 선언. **아무것도 그 유형을 내지 않음** |
| `5ab2bad` | v0.4 셋 60건 동결 + **구현 전 baseline 측정** |
| `6951b2e` | 엔진 구현 |
| (문서 커밋) | 재측정 + 문서 |

`fraud-holdout-v0.4-before-fix.json` 은 탐지 코드가 존재하기 전에 커밋됐다.
전/후 차이는 `git log` 에서 확인된다.

```bash
PYTHONPATH=. python scripts/evaluate_fraud_engine.py \
  --dataset evaluation/data/fraud_holdout_v0.4.jsonl \
  --output evaluation/results/fraud-holdout-v0.4.json
```

### 구현 전 / 구현 후 (held-out v0.4, 60건)

| 지표 | 구현 전 | 구현 후 |
|---|---:|---:|
| f1 | 0.6552 | **0.9459** |
| precision | 0.8636 | 0.9211 |
| recall | 0.5278 | **0.9722** |
| accuracy | 0.6667 | 0.9333 |
| 오탐률 (FPR) | 0.1250 | **0.1250** |
| 미탐 | 17건 | **1건** |
| 필수 signal coverage | 0.3214 | 0.6786 |
| 필수 action coverage | 0.8102 | 0.9927 |
| 상태 정책 정확도 | 0.5667 | 0.8333 |
| 근거 coverage | 1.0000 | 1.0000 |

| 유형 | 지지 | f1 전 | f1 후 | precision | recall |
|---|---:|---:|---:|---:|---:|
| `investment_scheme` | 13 | 0.000 | 0.880 | 0.917 | 0.846 |
| `acquaintance_impersonation` | 11 | 0.000 | 0.900 | 1.000 | 0.818 |

나머지 7개 유형은 전·후가 같다.

**오탐률이 움직이지 않은 것이 이 표의 핵심이다.** 투자 어휘는 지금까지 잰 어떤
어휘보다 정상 은행 문자와 겹친다("원금 보장"은 투자 권유에서 거짓이고 예금
안내문에서 사실이다). v0.4 정상 24건 중 12건이 일부러 그 겹치는 어휘를 달고 있는데
새로 깨진 문장이 없었다. 오탐 3건은 구현 전 baseline 에 이미 있던 것 그대로다.

v0.2·v0.3 held-out 과 개발셋 v0.1 도 다시 쟀다. **기존 수치가 한 자리도 바뀌지
않았고**, 새 유형 행이 지지 0 으로 추가된 것이 전부다.

### 설계 판단 세 가지

- **`familiar_person_claim` 은 호칭이 아니라 핑계를 잡는다.** "엄마야" 는 진짜
  엄마도 쓴다. 어휘를 자칭 관계에 걸면 정상 가족 문자가 전부 걸린다. 이 사기를
  성립시키는 것은 원래 알던 번호로 되걸 수 없게 만드는 핑계다.
- **`familiar_person_claim` 에는 요구 조건을 걸지 않았다.** v0.3 의 맨 명사 게이트와
  반대 판단인데, 틀렸을 때의 비용이 다르기 때문이다. 잘못 켜져도 사용자는 "원래 알던
  번호로 전화해 보세요" 를 듣고 상대가 진짜면 그 전화는 연결된다. 통화 한 통이 비용의
  전부인 신호에 요구 조건을 걸어 진짜 사칭을 놓치는 쪽이 비싸다.
- **`private_channel_invite` 는 맨 명사를 쓰지 않는다.** "리딩방" 은 피해자
  자기보고와 예방 안내문에도 나온다. 신호는 방 이름이 아니라 읽는 사람을 그 방에
  들여놓으려는 동작이다. **이 결정은 v0.4 셋을 쓰기 전에 내렸다.**

### 새 액션 `VERIFY_BY_KNOWN_CONTACT`

`VERIFY_OFFICIAL_CHANNEL` 로 묶지 않았다. 다른 사칭은 기관을 자칭하므로 확인할
공식 대표번호가 존재하지만 **자칭 지인에게는 대표번호가 없다.** 같은 항목으로
묶으면 사용자에게 존재하지 않는 창구를 찾으라고 말하게 된다.

근거는 새로 만들지 않고 이미 검증된 `police_1394` 에 붙였다. 투자 사기의 자연스러운
근거는 금융감독원 파인 제도권금융회사조회지만 URL 을 확인하지 못했고, 확인 못 한
공식 근거를 다느니 유형만 이름 붙이는 쪽을 택했다.

### 이 셋에서 찾았지만 고치지 않은 것

재는 셋 안의 결함은 고치지 않는다. 고치면 그 셋은 성능이 아니라 기억을 재게 된다.

| 사례 | 놓친 것 | 원인 |
|---|---|---|
| `fh-223` | `acquaintance_impersonation` | 어휘 "폰이 고장" vs 문장 "폰 고장났어" |
| `fh-227` | `acquaintance_impersonation` | "폰을 잠깐 못 써"·"다른 계정으로 연락" 미수록 |
| `fh-206` | `investment_scheme` | 어휘 "확정 수익" vs 문장 "5배는 확정" |
| `fh-215` | `investment_scheme` | 어휘 "오픈채팅방으로" vs 문장 "오픈채팅방 입장" |
| `fh-219` | `account_access_request` | `DEMAND_MARKERS` 가 반말 "보내 줘" 를 못 덮음 |

설계 결함이 아니라 활용형·어휘 폭 문제이며, v0.3 의 `옮기`/`옮겨` 와 같은 부류다.
`fh-219` 는 공유 요구 게이트를 건드리므로 오탐을 따로 재야 한다.

오탐 3건도 그대로 둔다. `fh-245`·`fh-260` 은 "추천하지 않습니다"·"하지 않습니다" 가
`PREVENTION_STATEMENT_MARKERS` 에 없어 예방 안내문이 사칭으로 잡히는 것이고,
`fh-252` 는 과거형 자기보고에서 `ACTION_DEMAND_PATTERN` 이 "제출+하" 를 요구로
읽는 것이다. `fh-260` 은 이번에 `investment_scheme` 이 추가로 달렸고, 이미 오탐이던
문장이라 이진 FPR 은 그대로지만 `investment_scheme` precision 0.917 의 유일한
원인이다.

> **v0.4 소진 예정.** 위 여덟 건을 고치려면 **v0.5 를 먼저 얼려야 한다.**

### 다음에 필요한 것 (v0.4 시점)

- **held-out v0.5 동결.** 위 8건을 고치기 전에.
- **`HIGH_RISK_SIGNAL_COMBINATIONS` 기존 조합 확장.** 이번에는 새 신호가 들어간
  조합만 추가했다. 기존 신호끼리를 같이 손대면 v0.3 수치가 무엇 때문에 움직였는지
  갈라낼 수 없다. v0.3 의 10건이 여전히 대상이다.
- **판정 프롬프트 v0.2 와 유료 재실행.** 이제 3종(`advance_fee_demand`,
  `investment_scheme`, `acquaintance_impersonation`)이 프롬프트 밖에 있다.

## 2026-08-21 절 범위 요구 결합과 held-out v0.5

v0.4 가 남긴 결함 8건을 고쳤다. **고치기 전에 v0.5 를 얼렸다.** 상세는
`devlog/2026-08-21/fraud-clause-scoped-demand.md`.

| 커밋 | 내용 |
|---|---|
| `8930328` | v0.5 셋 72건 동결 + **수정 전 baseline 측정** |
| `107ade4` | 엔진 수정 네 가지 |
| `edf0a30` | 필수 signal 코드 이름 오류 수정 + 재발 방지 검증 |
| (문서 커밋) | 재측정 + 문서 |

```bash
PYTHONPATH=. python scripts/evaluate_fraud_engine.py   --dataset evaluation/data/fraud_holdout_v0.5.jsonl   --output evaluation/results/fraud-holdout-v0.5.json
```

### 수정 전 / 수정 후 (held-out v0.5, 72건)

| 지표 | 수정 전 | 수정 후 |
|---|---:|---:|
| f1 | 0.6250 | **0.9348** |
| precision | 0.6944 | 0.8958 |
| recall | 0.5682 | **0.9773** |
| accuracy | 0.5833 | 0.9167 |
| 오탐률 (FPR) | 0.3929 | **0.1786** |
| 미탐 | 19건 | **1건** |
| 필수 signal coverage | 0.3934 | 0.9836 |
| 필수 action coverage | 0.7554 | 0.9856 |
| 상태 정책 정확도 | 0.7361 | 0.9861 |
| 근거 coverage | 1.0000 | 1.0000 |

9개 유형 중 5개가 올랐고(`acquaintance_impersonation` 0.286 → 0.909,
`investment_scheme` 0.400 → 0.923, `account_access_request` 0.538 → 0.929,
`smishing_malware` 0.667 → 0.933, `authority_impersonation` 0.750 → 0.933),
`money_mule_transfer` 는 0.800 → 1.000, 나머지 3개는 1.000 유지다.

**수정 전 오탐률 0.3929 가 v0.4 의 세 배인 것은 셋을 그렇게 만들었기 때문이다.**
이번 수정 네 가지는 전부 재현율을 오탐으로 사는 종류라, 72건 중 28건이 그 네 가지를
정확히 깨도록 고른 정상 문장이다. 이 표의 오탐률은 현장 추정치가 아니라 **가격표**다.

### 다른 네 셋 (회귀 확인)

| 셋 | f1 | precision | FPR | 미탐 |
|---|---:|---:|---:|---:|
| 개발셋 v0.1 (61건) | 1.0000 → 1.0000 | 1.0000 | 0.0000 | 0 |
| held-out v0.2 (72건) | 0.9545 → **0.9767** | 0.9545 → **1.0000** | 0.0714 → **0.0000** | 2 |
| held-out v0.3 (60건) | 0.9836 → 0.9836 | 1.0000 | 0.0000 | 1 |
| held-out v0.4 (60건) | 0.9459 → **0.9863** | 0.9211 → **0.9730** | 0.1250 → **0.0417** | 1 → **0** |

v0.2 오탐 0건, v0.4 미탐 0건으로 둘 다 역대 최고다. v0.3 과 개발셋은 불변.

### 네 가지 수정의 핵심

- **활용형.** 어휘가 한 형태만 적혀 있었다("폰이 고장" 은 있고 "폰 고장" 은 없음).
  한국어 어미가 앞 음절에 합쳐지므로 기본형이 활용형의 접두사가 아니다 —
  "메워 드리" 는 "메워 드립니다" 를 못 잡는다. 이번에 실제로 한 번 밟았다.
- **반말 요구.** `DEMAND_MARKERS` 가 "-아/어 주세요" 계열만 덮어서, 요구 게이트가
  반말 문자에서 통째로 닫혀 있었다. 지인 사칭은 거의 전부 반말로 오므로 **가장
  잡아야 할 사기에서 게이트가 가장 자주 안 열렸다.**
- **예방 안내문.** 어간이 붙은 서술형만 넣는다. 맨 "하지 않" 을 넣으면 "소명하지
  않으면 계좌가 동결됩니다" 가 예방 안내문이 된다.
- **절 범위.** 게이트에 범위가 아예 없었다. **거리로는 못 가른다** — `fh-321`(사기)과
  `fh-356`(정상)은 요구·대상 거리가 같고, 진짜 사기는 요구를 마지막 절에 몰아 쓴다.
  가르는 것은 **절의 시제**이며, 절 끝에 붙은 과거형만 센다.

### 이 셋에서 찾았지만 고치지 않은 것

| 사례 | 상태 | 이유 |
|---|---|---|
| v0.5 `fh-308` | 미탐 | `단톡방 초대` 가 v0.4 `fh-244`(정상)와 구절이 같다. v0.4 가 남긴 판단("무엇을 위한 초대인가")을 새 셋 점수를 위해 뒤집지 않는다 |
| v0.5 `fh-358`·`fh-360`·`fh-361` | 오탐 | 어느 절이 보고인지만 다르고 `fh-323`·`fh-324` 와 표면이 같다 |
| v0.5 `fh-352`·`fh-354` | 오탐 | 각각 `알려` 를 요구 표지로 둔 값, `familiar_person_claim` 에 요구 조건을 안 건 값. 둘 다 이전에 기록된 판단의 가격 |
| v0.2 `fh-005` | 신호 1개 손실 | 기관명이 진짜 완료 보고절 안에 있다. 범위 버그가 아니라 `authority_impersonation` 이 *신분*을 요구 조건에 건 모양 문제 |

### 재는 자 자체의 버그 — 필수 signal 코드 이름

`project_public_signals` 가 내부 규칙 코드 6개를 응답에서 다른 이름으로 바꿔
내보내는데(`account_access_request` → `account_access` 등), v0.4·v0.5 셋이
**내부 이름**으로 적혀 있었다. 어떤 엔진으로도 만족될 수 없는 조건이므로
`required_signal_coverage` 는 탐지가 아니라 이름 불일치를 재고 있었다.

v0.4 11건·v0.5 24건을 응답이 싣는 이름으로 고쳤다. 이진 지표·action coverage·
정책 정확도는 이 필드를 읽지 않으므로 영향이 없다.

| 셋 | signal coverage (이름 수정 전 → 후) |
|---|---:|
| held-out v0.4 | 0.7143 → 0.9643 |
| held-out v0.5 | 0.5410 → 0.9836 |

허용 목록은 이제 규칙 표와 투영 맵에서 끌어오고, 검증기가 응답이 실을 수 없는
코드를 요구하는 사례를 거부한다.

> **v0.5 소진.** 결함이 이름으로 적혔으므로 다음 수정부터는 기억을 재게 된다.

### 다음에 필요한 것 (v0.5 시점)

- **held-out v0.6 동결.** 아래를 고치기 전에.
- **`private_channel_invite` 의 투자·매매 맥락 조건.** `fh-244` / `fh-308` 을 갈라야
  단톡방 초대 어형을 넣을 수 있다.
- **`authority_impersonation` 의 게이트 모양.** 신분 주장에 요구 조건을 거는 것이
  맞는지. v0.2 의 `fh-004`·`fh-005`·`fh-012`·`fh-013` 이 전부 여기 걸려 있다.
- **`HIGH_RISK_SIGNAL_COMBINATIONS` 기존 조합 확장.** v0.3 의 10건. 탐지 변경과 정책
  변경을 한 커밋에 섞으면 귀속이 무너져서 이번에도 묶지 않았다.
- **판정 프롬프트 v0.2 와 유료 재실행.** 3종이 여전히 프롬프트 밖에 있다.

## 2026-08-22 자칭 신분의 요구 게이트와 held-out v0.6

v0.5 가 남긴 결함 세 가지를 고쳤다. **고치기 전에 v0.6 을 얼렸다.** 상세는
`devlog/2026-08-22/fraud-request-gated-identity.md`.

| 커밋 | 내용 |
|---|---|
| `ef60a5e` | v0.6 셋 72건 동결 + **수정 전 baseline 측정** |
| `7688dad` | 엔진 수정 세 가지 + 회귀 보수 7건 + 전 셋 재측정 |
| (문서 커밋) | 문서 |

```bash
PYTHONPATH=. python scripts/evaluate_fraud_engine.py \
  --dataset evaluation/data/fraud_holdout_v0.6.jsonl \
  --output evaluation/results/fraud-holdout-v0.6.json
```

### 이번 회차는 수정의 방향이 반대다

v0.2 부터 v0.5 까지 잰 수정은 전부 **넓히는** 것이었다. 넓히기의 값은 정상 문장에서
치러지므로 안전장치는 **부정 사례**였고, v0.5 는 72건 중 28건을 그 값으로 썼다.

이번 세 가지 중 둘은 **좁히는** 것이다 — 기관 자칭 게이트에 민감 요구 조건을 걸고,
공식 창구를 가리키는 절을 요구에서 뺀다. **좁히기의 값은 정상 문장이 아니라 사기
쪽에서 치러진다.** 오탐률이 내려가는데 그 대가가 표에 안 보이면, 그 표는 개선을
보여 주는 것이 아니라 개선을 숨기는 것이다.

그래서 v0.6 의 안전장치는 부정 사례 수가 아니라 **좁히기가 끊을 수 있는 자리에 놓인
양성 사례 수**이고, 통합 테스트가 그것을 센다
(`test_holdout_v0_6_prices_two_narrowings_with_fraud_not_with_normal_text`).

### 수정 전 / 수정 후 (held-out v0.6, 72건)

| 지표 | 수정 전 | 수정 후 |
|---|---:|---:|
| f1 | 0.7838 | **0.9429** |
| precision | 0.7632 | **0.9706** |
| recall | 0.8056 | 0.9167 |
| accuracy | 0.7778 | 0.9444 |
| 오탐률 (FPR) | 0.2500 | **0.0278** |
| 미탐 | 7건 | 3건 |
| 오탐 | 9건 | **1건** |
| 필수 signal coverage | 0.8246 | 0.9649 |
| 필수 action coverage | 0.8943 | 0.9837 |
| 상태 정책 정확도 | 0.8333 | 0.9028 |
| 근거 coverage | 1.0000 | 1.0000 |

유형별로는 셋이 올랐다 — `authority_impersonation` 0.526 → **0.947**,
`investment_scheme` 0.545 → **1.000**, `account_access_request` 0.815 → 0.917.
`money_mule_transfer` 는 0.667 그대로이고 나머지 다섯은 1.000 유지다.

같은 셋에서 legacy 5키워드 baseline 은 f1 0.6154(미탐 20건)다.

### 다른 다섯 셋 (회귀 확인)

| 셋 | f1 | precision | FPR | 미탐 | signal cov |
|---|---:|---:|---:|---:|---:|
| 개발셋 v0.1 (61건) | 1.0000 → 1.0000 | 1.0000 | 0.0000 | 0 | 0.9811 |
| held-out v0.2 (72건) | 0.9767 → 0.9767 | 1.0000 | 0.0000 | 2 | 0.9016 → **0.9508** |
| held-out v0.3 (60건) | 0.9836 → 0.9836 | 1.0000 | 0.0000 | 1 | 0.9811 |
| held-out v0.4 (60건) | 0.9863 → 0.9863 | 0.9730 | 0.0417 | 0 | 0.9643 |
| held-out v0.5 (72건) | 0.9348 → **0.9670** | 0.8958 → **0.9362** | 0.1786 → **0.1071** | 1 → **0** | 0.9836 → **1.0000** |

**v0.5 가 이번 수정으로 만점 구간에 들어왔다.** v0.5 회차에서 「고치지 않은 것」으로
적어 둔 `fh-308`(미탐)·`fh-360`·`fh-361`(오탐)이 사라졌다. 그때 적어 둔 진단이
맞았다는 뜻이고, **새 셋을 얼린 뒤에 고쳤으므로 기억을 잰 것이 아니다.**

### 세 가지 수정의 핵심

- **자칭 신분은 요구의 대상이 아니다.** `authority_impersonation` 의 조건부 목록
  (은행·카드사·공단)이 "메시지에 요구가 있는가" 로 켜지고 있었다. 정상 안내문도 하루
  종일 무언가를 요구한다 — v0.6 오탐 9건 중 4건이 전부 기관 + **무해한** 요구였다.
  그래서 (a) 자칭은 절 시제로 가릴 대상이 아니므로 **메시지 전체**에서 찾고,
  (b) 게이트를 요구의 **존재**가 아니라 요구의 **내용**(인증정보·계좌·앱 설치·원격·
  송금·의심스러운 링크)으로 좁혔다. 구현은 규칙 표에 `request_gated_keywords` 를 두고
  탐지를 두 바퀴로 나눈 것이다 — 한 바퀴면 규칙 표의 **순서**가 판정을 바꾼다.
- **정상 기관은 이미 아는 창구로 보내고, 사기는 자기가 만든 창구로 부른다.** 공식
  창구를 가리키는 절은 읽는 사람을 이 메시지 **밖**으로 내보내므로, 이 메시지가
  무엇을 받아 가려는지에 대해 아무 말도 하지 않는다. 완료 보고절과 나란히 열린 절에서
  뺐다. **절 단위로만 본다** — 메시지 단위면 안전한 첫 문장 하나로 나머지가 통과한다.
- **방 이름의 활용형을 쫓는 일은 끝이 없다.** 단톡방·오픈채팅방·텔레그램을 조사 붙은
  형태로 적어 두는 것으로는 아무것도 갈리지 않는다. 맨 이름을 새 게이트
  `investment_gated_keywords` 로 내렸다. 매매 맥락 어휘에 **"투자"와 "주식"은 넣지
  않았다** — 정상 문장이 매일 쓰는 말이라 방 이름과 만나면 학부모 단톡방이 걸린다.
  리딩방·종목추천방 계열은 이름 자체가 매매를 말하므로 조건 없이 남겼다.

### 회귀 7건을 냈고 7건 다 보수했다

좁히기를 넣고 처음 전 셋을 돌렸을 때 v0.2 3건·v0.3 1건·v0.4 1건·v0.5 2건이 새로
미탐이 됐다. **전부 이미 소진된 셋에서 나왔다.** 소진된 셋으로 수정을 이끄는 것은
규칙 위반이 아니다 — 규칙이 금지하는 것은 **이번에 재고 있는 셋**(v0.6)의 오답을 보고
고치는 일이고, v0.6 의 남은 4건은 손대지 않았다.

- **기관 + 자기가 만든 창구로 부르기**(`fh-007`·`fh-011`·`fh-109`) — 창구 안내 규칙의
  **거울상**이다. `fh-109` 는 "카드사 대표번호 말고 아래 담당자 번호로" 라고 공식
  창구를 명시적으로 밀어낸다. 민감 요구 목록에 넣었다.
- **재인증 요구**(`fh-010`) — 본인 인증 어휘의 구멍.
- **의심스러운 링크**(`fh-156`, 주입 골든셋 `pi-002`) — 어휘적으로 의심스러운 링크
  자체를 민감 요구로 친다. 진짜 기관은 단축 URL 로 보내지 않는다.
- **매매 맥락 어휘 구멍**(`fh-214`·`fh-307`·`fh-309`) — `매매`·`장 마감`·`시황`·`종가`.

### 이 셋에서 찾았지만 고치지 않은 것

| 사례 | 상태 | 이유 |
|---|---|---|
| v0.6 `fh-438` | 미탐 | **좁히기의 값.** 민감 명사가 창구 안내 절 안에 있다. 살리려면 창구 규칙을 되돌려야 하고 그러면 `fh-430`~`fh-432` 가 되돌아온다 |
| v0.6 `fh-446` | 미탐 | "그 돈 빼서 다른 계좌로 넣어" — `receive_and_forward_money` 어휘 구멍. v0.3 `fh-138` 과 같은 자리 |
| v0.6 `fh-454` | 미탐 | `secrecy_isolation` 신호는 켜지는데 대응하는 **사기 유형이 없다.** 분류 체계의 공백이지 탐지 실패가 아니다 |
| v0.6 `fh-472` | 오탐 | 동호회 회비 공지. 계좌번호 + 입금 요구가 정말로 있다. 가르려면 서비스 범위 밖의 관계 정보가 필요하다 |
| v0.6 정책 7건 중 6건 | 등급 부족 | `fh-415`·`fh-416`·`fh-418`~`fh-421`. 신호는 다 맞는데 `authority_impersonation` + 넘겨주기 신호 조합이 `HIGH_RISK_SIGNAL_COMBINATIONS` 에 없다. **탐지가 아니라 가격표 문제**라 커밋을 섞지 않았다 |

> **v0.6 소진.** 위 결함이 이름으로 적혔으므로 다음 수정부터는 기억을 재게 된다.

### 다음에 필요한 것 (v0.6 시점)

- **held-out v0.7 동결.** 아래를 고치기 전에.
- **`HIGH_RISK_SIGNAL_COMBINATIONS` 확장.** v0.3 의 10건 + v0.6 의 6건. 정책 단독 커밋.
- **`receive_and_forward_money` 어휘.** `fh-138`·`fh-446`.
- **`secrecy_isolation` 에 대응하는 사기 유형.** taxonomy 확장이므로 v0.4 와 같은 모양.
- **판정 프롬프트 v0.2 와 유료 재실행.** 3종이 여전히 프롬프트 밖에 있다.

## 2026-08-22 재는 자의 천장과 held-out v0.7

v0.6 이 남긴 결함 셋을 고쳤다. **고치기 전에 v0.7 을 얼렸고, 얼리기 전에 지표부터
고쳤다.** 상세는 `devlog/2026-08-22/fraud-ceiling-and-grade-raising.md`.

| 커밋 | 내용 |
|---|---|
| `0ce5188` | 지표에 천장 추가 + v0.7 72건 동결 + **수정 전 baseline 측정** |
| `af358c4` | `isolation_coercion` 유형 + `secrecy_isolation` 어휘 정리 |
| `83298e8` | `receive_and_forward_money` 어휘 + 요구 조건 |
| `8257632` | `HIGH_RISK_SIGNAL_COMBINATIONS` 확장 (정책 단독 커밋) |
| `cd9eea4` | 여덟 셋 전체 재측정 |

```bash
PYTHONPATH=. python scripts/evaluate_fraud_engine.py \
  --dataset evaluation/data/fraud_holdout_v0.7.jsonl \
  --output evaluation/results/fraud-holdout-v0.7.json \
  --performance-repeats 3
```

### 이번 회차는 재는 자를 먼저 고쳤다

v0.2~v0.5 가 잰 것은 **넓히기**이고 그 값은 정상 문장에서 치러진다. v0.6 이 잰 것은
**좁히기**이고 그 값은 사기 쪽에서 치러진다. 이번 셋 가운데 하나는 **올리기**인데,
`_scenario_policy_accuracy` 가 `expected_min_risk` 에 대해 `>=` 만 보므로 **모든
메시지에 `high` 를 찍는 엔진이 1.0 을 받는다.** 올리는 수정이 점수를 잃을 자리가 이
프로젝트에 한 곳도 없었다.

`FraudGoldenCase.expected_max_risk` 와 보고서의 `risk_ceiling_accuracy` 를 넣었다.
두 가지 규칙이 붙는다.

- **천장은 정상 문장에만 선언한다.** 사기를 필요 이상으로 높게 매기는 것은 결함이
  아니라 신중함이고, 거기에 천장을 씌우면 신중함에 벌점을 주게 된다.
- **재지 않은 것은 만점이 아니다.** 천장을 선언하지 않은 셋에서 이 지표는 `1.0` 이
  아니라 `None` 이다. 분모가 0일 때 1.0 을 적는 것은 "재지 않았다" 를 "완벽했다" 로
  바꿔 적는 일이고, CLAUDE.md 가 금지하는 지어낸 근거다.

필드를 추가하자 **기존 여섯 셋의 `normalized_dataset_sha256` 이 전부 바뀌었다** —
유료 LLM 판정이 붙어 있는 개발셋까지 포함해서다. `expected_max_risk` 가 `None` 이면
신원 계산에서 뺀다. `held_out` 을 이미 같은 이유로 빼고 있었다. **선언하지 않은 라벨은
라벨이 아니다.**

### 수정 전 / 수정 후 (held-out v0.7, 72건)

두 결과 파일은 같은 `dataset.normalized_sha256`(`15286971f1...`)을 갖는다.

| 지표 | 수정 전 | 수정 후 |
|---|---:|---:|
| f1 | 0.7241 | **0.8923** |
| precision | 0.9545 | **1.0000** |
| recall | 0.5833 | **0.8056** |
| accuracy | 0.7778 | 0.9028 |
| 오탐률 (FPR) | 0.0278 | **0.0000** |
| 미탐 | 15건 | **7건** |
| 오탐 | 1건 | **0건** |
| 필수 signal coverage | 0.6078 | 0.7059 |
| 필수 action coverage | 0.8194 | 0.8611 |
| 상태 정책 정확도 | 0.7361 | **0.8750** |
| **등급 천장 정확도** | 0.8611 | **1.0000** |
| 근거 coverage | 1.0000 | 1.0000 |

유형별로 둘이 0 에서 올라왔다 — `isolation_coercion` 0.000 → **0.933**(support 8),
`money_mule_transfer` 0.000 → **0.545**(support 8). legacy 5키워드 baseline 은 f1 0.5.

### 동결 시점 baseline 이 수정의 값을 미리 보여 줬다

새 천장 지표가 얼리자마자 제 값을 했다. `secrecy_isolation` 이 평범한 비밀 유지 문장
넷(`fh-527`·`fh-528`·`fh-530`·`fh-531`)에서 이미 켜져 등급을 medium 으로 올리고
있었다. 지금은 과대 등급일 뿐이지만, 이진 판정이 `bool(fraud_types)` 이므로 이 신호에
사기 유형을 붙이는 **그 순간 네 건이 전부 사기 판정이 된다.** 그래서 유형과 어휘를 한
커밋에서 함께 바꿨고, **수정의 값이 수정 전에 기록됐다.**

### 다른 여섯 셋 (회귀 확인)

| 셋 | f1 | 미탐 | 상태 정책 |
|---|---:|---:|---:|
| 개발셋 v0.1 (61건) | 1.0000 (불변) | 0 | 1.0000 |
| held-out v0.2 (72건) | 0.9767 (불변) | 2 | 0.7083 → **0.8194** |
| held-out v0.3 (60건) | 0.9836 → **1.0000** | 1 → **0** | 0.8333 → **0.9333** |
| held-out v0.4 (60건) | 0.9863 (불변) | 0 | 0.8500 → **0.8667** |
| held-out v0.5 (72건) | 0.9670 (불변) | 0 | 1.0000 |
| held-out v0.6 (72건) | 0.9429 → **0.9722** | 3 → **1** | 0.9028 → **0.9861** |

**v0.3 이 이진 판정 만점 구간에 들어왔고**(`fh-138`), **v0.6 은 미탐 3건이
1건이 됐다**(`fh-446`·`fh-454`). 셋 다 v0.6 회차에서 「고치지 않은 것」으로 이름을
적어 둔 사례다.

정책 커밋은 **네 셋에서 동시에** 상태 정책 정확도를 올렸다. 옳은 등급 규칙은 그렇게
생겼다 — 자기가 겨냥한 한 셋만이 아니라 그 모양이 들어 있는 모든 셋을 움직인다.
등급이 오른 것은 일곱 셋 전체에서 **39건이고 39건 전부 사기다.** 정상 문장은 한 건도
오르지 않았고 천장도 한 번도 넘지 않았다.

### 세 가지 수정의 핵심

- **비밀 유지는 정상이고, 확인 차단이 신호다.** `secrecy_isolation` 은 v0.1 부터
  켜졌지만 사기 유형이 없어서, 이진 판정이 `bool(fraud_types)` 인 이상 등급만 올리고
  판정은 정상으로 나갔다(v0.6 `fh-454`). 유형을 붙이려면 어휘를 먼저 고쳐야 했다 —
  기밀 유지는 화제를 집단 안에 두는 일이고 정상 문서가 매일 한다. 사기가 요구하는
  것은 **제3자 확인의 봉쇄**이고, 어형은 전칭 금지(아무에게도·누구에게도·주변에·
  가족에게도)이거나 창구 봉쇄(통화를 끊지 마·저와만 연락)다. `혼자 처리` 는 읽는
  사람에게 시키는 형태만 남겼다 — **같은 낱말이 주어에 따라 갈린다.**
- **재전달을 만드는 것은 동사도 목적어도 아니고 요구다.** `넘겨` 가 어휘에 없었고
  (`fh-138`), `빼서 넣어` 는 **붙여** 적혀 있어서 목적지가 끼는 실제 문장을 두 회차
  동안 한 번도 잡지 못했다(`fh-446`). 순서만 지키면 받는 `money_gated_sequences` 로
  고쳤다. 둘 다 넓히기이므로 값을 같은 커밋에서 치른다 — 금액 조건은 `자동이체` 의
  `이체` 로도 차기 때문에 "적금 계좌로 옮겨 놓을게"(`fh-545`)가 동결 시점에 이미
  오탐이자 천장 초과였다. 조건에 요구를 더했고, 그러자 **요구가 동사에 붙어 있는
  자리**(`옮기세요`)가 드러나 게이트 안에서만 명령형 어미를 읽게 했다.
- **기관은 자칭과 같은 메시지에서 넘겨 달라고 하지 않는다.** 자칭도 medium 하한, 민감
  요구도 medium 하한인데 `max` 는 두 medium 을 더하지 않는다. 소진된 다섯 셋에 이
  모양의 하한 미달이 15건 있었다. **짝을 사례에서 세지 않고 `SENSITIVE_REQUEST_SIGNALS`
  에서 가져왔다** — 그 목록이 이미 "기관이 문자로 시키지 않는 요구"의 정의다. 그
  집합을 parametrize 한 테스트가 두 표의 분기를 막는다.

### 이 셋에서 찾았지만 고치지 않은 것

| 사례 | 상태 | 진단 |
|---|---|---|
| `fh-508` | 미탐 | 신호가 하나도 안 켜진다. 자칭은 민감 요구를 기다리고 `계좌 접근 권한을 위임` 은 어휘에 없다. **두 게이트가 서로를 기다린다** |
| `fh-523` | 미탐 | `저와만 연락` 은 어휘에 있는데 `드리지 않` 이 예방 안내문으로 읽혀 문장이 눌린다. v0.5 억제기의 과잉 |
| `fh-539` | 미탐 | `정산금` 이 `MONEY_OBJECT_TERMS` 에 없다 |
| `fh-542` | 미탐 | `돌려보내` 가 어휘에 없다 |
| `fh-543` | 미탐 | `나눠 보내` 가 어휘에 없다 |
| `fh-561` | 미탐 | `지검` 이 기관 어휘에 없다 |
| `fh-562` | 미탐 | 선입금 요구인데 `money_transfer_request` 가 안 켜진다 |

`risk_score` 와 `risk_level` 이 갈라져 있는 것도 이 회차에 기록만 했다.
`baseline_score` 는 legacy 5규칙 가중치만 더하는데 등급은 canonical 신호에서 나오므로
**`risk_score: 0` 인 `high` 응답이 나갈 수 있다.**

> **v0.7 소진.** 위 결함이 이름으로 적혔으므로 다음 수정부터는 기억을 재게 된다.

### 다음에 필요한 것 (v0.7 시점)

- **held-out v0.8 동결.** 위 7건을 고치기 전에.
- **자칭 게이트의 순환.** `fh-508`.
- **예방 안내문 억제기의 과잉.** `fh-523`.
- **`receive_and_forward_money` 남은 어형과 `MONEY_OBJECT_TERMS`.** `fh-539`·`fh-542`·`fh-543`.
- **`risk_score` 를 canonical 신호에서 계산하거나 이름을 바꾸거나.**
- **판정 프롬프트 v0.2 와 유료 재실행.** 이제 **4종**이 프롬프트 밖에 있다.

## 지연시간 범위

결과 JSON의 latency는 FastAPI 앱을 같은 Python 프로세스에서 ASGI transport로 호출한
개발기 측정이다. 네트워크, TLS, reverse proxy, 실제 동시 사용자, 외부 상품 API
지연은 포함하지 않는다. 운영 SLO나 배포 성능 수치로 사용하지 않는다.
