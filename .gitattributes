# 셸 스크립트는 호스트 작업 트리에서 컨테이너로 그대로 bind mount 된다
# (`compose.yaml`의 `backup` 서비스). Windows 는 `core.autocrlf=true` 가 기본이라
# 그냥 두면 체크아웃할 때 CRLF 로 바뀌고, 컨테이너의 `/bin/sh` 는 줄 끝의 `\r` 을
# 명령의 일부로 읽는다. 백업 컨테이너가 로직이 아니라 `not found` 로 죽는데,
# 파일을 열어보면 멀쩡해 보여서 원인을 짚기 어렵다.
*.sh text eol=lf
